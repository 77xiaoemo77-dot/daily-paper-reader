Title: Quantum Kernels for Parity-Structured Classification: A Hybrid Pipeline

URL Source: https://arxiv.org/pdf/2605.05625v1

Published Time: Fri, 08 May 2026 00:28:18 GMT

Number of Pages: 6

Markdown Content:
# Quantum Kernels for Parity-Structured Classification: A Hybrid Pipeline 

Tushar Pandey 

Texas A&M University, Department of Mathematics College Station, TX, USA 

tusharp@tamu.edu ORCID: 0000-0001-7448-5723 

Abstract —Parity (XOR) classification requires detecting dis-crete, high-order feature interactions that smooth classical kernels cannot efficiently capture. We study how quantum kernel advantage depends on parity complexity —the number of features entering the XOR rule—and find a clear threshold behavior. We pair a ZZ quantum feature map with binary {0, π }

encoding (features median-thresholded before circuit input) to expose parity structure. A binary encoding ablation—RBF SVM trained on the identical {0, π } features—separates encoding from circuit effects: at low complexity ( n = 5 features), binary RBF achieves 83 .4% ± 1.7% and the quantum kernel 81 .2% ± 1.9% ,showing encoding drives performance there. At high complexity (n = 11 features, 11 qubits, r = 3 ZZ repetitions), all classical methods collapse to near-random ( ∼50%), binary RBF reaches only 54 .3% ± 1.1% , and the quantum ZZ kernel achieves 

66 .3% ± 3.2% (mean ± std, 10 seeds)—a +12 .0 percentage-point margin over the binary ablation and ∼7× higher kernel–target alignment ( 0.094 ± 0.020 vs. 0.013 ± 0.001 ). These results identify parity complexity as a concrete axis along which genuine quantum kernel advantage, not attributable to encoding alone, emerges. 

Index Terms —quantum machine learning, quantum kernels, support vector machines, parity functions, feature maps 

I. I NTRODUCTION 

High-dimensional classification with non-linear structure remains a fundamental challenge in machine learning. We study a synthetic parity-structured benchmark inspired by the NIPS 2003 feature selection challenge [1]: samples are drawn from a 500-dimensional space with n informative features; class labels are determined by the parity (XOR) of those n features after median thresholding, placing points on vertices of an 

n-dimensional hypercube. (The original UCI Madelon dataset has a different label structure; our benchmark is synthetic and parity-based.) A key question is: how does quantum kernel advantage depend on n, the parity complexity? 

Classical kernel methods such as the Radial Basis Function (RBF) and polynomial kernels have limited success on parity-structured data [2]. This limitation arises from a fundamental bias: these kernels measure similarity through Euclidean distance or low-degree polynomial interactions, which are poorly suited to the discrete, high-order correlations required for parity detection. The XOR function y = x1 ⊕ x2 ⊕ · · · ⊕ xn

creates a checkerboard-like decision boundary that defies 

Accepted at IEEE DCAS 2026. This is the author’s accepted manuscript; the published version is available via IEEE Xplore. 

smooth approximation. Moreover, even if features are binary-thresholded before kernel computation, the resulting Hamming-based similarity becomes less informative as n grows, because the number of distinct parity-class configurations scales as 2n

while Hamming distance scales only linearly. Recent advances in quantum computing [7], [11] have introduced quantum kernels as a potential solution. Havl ´ı ˇcek et al. [3] demonstrated that quantum feature maps encode data into high-dimensional Hilbert spaces where certain non-linear patterns become linearly separable, and Schuld [6] established a formal equivalence between supervised quantum models and kernel methods. The ZZ feature map, which implements ZiZj

entangling gates between qubits, naturally encodes pairwise correlations through quantum phase interference. Crucially, the resulting Hilbert space has dimension 2n, growing exponen-tially with the number of qubits—matching the exponential complexity of the parity classification landscape. However, Liu et al. [8] showed that quantum speedup in kernel methods requires careful problem structure alignment, precisely the insight we operationalize here. In this work, we make four key contributions: 1) We implement a hybrid classical-quantum pipeline with direct informative-feature selection, a ZZ quantum feature map, and binary {0, π } encoding; we show that RFE fails on parity data (zero marginal information per feature) and identify oracle selection as a prerequisite for fair comparison. 2) We introduce a binary encoding ablation—RBF SVM trained on the same {0, π } features as the quantum kernel—to cleanly separate the contribution of prepro-cessing from the quantum circuit itself. 3) We conduct a systematic sweep of parity complexity n ∈{5, . . . , 11 }, identifying n ≈ 11 as the threshold above which the quantum circuit provides genuine advantage beyond encoding. 4) At n = 11 (11-way XOR, 11 qubits, r = 3 , 10 seeds, 800 samples): quantum ZZ achieves 66 .3% ± 3.2% vs. 

54 .3% ± 1.1% for binary RBF and ∼50% for all five continuous baselines (Linear SVC, RBF SVC, Random Forest, XGBoost, MLP), with ∼7× higher kernel–target alignment ( 0.094 vs. 0.013 ). 

> arXiv:2605.05625v1 [quant-ph] 7 May 2026

II. R ELATED WORK 

A. Quantum Kernel Methods 

Quantum kernel methods leverage quantum computers to compute inner products in exponentially large Hilbert spaces. Havl ´ı ˇcek et al. [3] introduced the paradigm of quantum-enhanced feature spaces, showing that quantum feature maps can create kernel functions that are computationally hard for classical computers to simulate efficiently. Schuld and Killoran [4] provided a formal connection between quantum circuits and kernel methods, demonstrating that quantum kernels satisfy Mercer’s theorem and can be integrated seamlessly with classical support vector machines. The ZZ feature map, which applies Pauli-Z rotations and controlled-ZZ entangling gates, has been shown to capture multivariate polynomial features up to degree 2r for r

repetitions [3]. This makes it particularly well-suited for problems requiring high-order feature interactions, such as parity functions. However, previous work has primarily focused on continuous feature encoding via rotation angles proportional to feature values, leaving unexplored the potential of discrete encodings tailored to specific problem structures. 

B. Parity-Structured Benchmarks and Classical Baselines 

Benchmarks that place informative features on hypercube vertices with parity-derived labels (as in the NIPS 2003 feature selection setting [1]) are resistant to feature selection based on univariate statistics and pose a challenge for smooth kernels. Classical approaches on such or related settings have achieved varying degrees of success: Guyon et al. [2] reported around 62% accuracy with careful feature selection and ensemble methods. Random forests and gradient boosting typically reach 60–65% due to non-linear interactions, but struggle with high dimensionality and parity structure [2]. Our synthetic benchmark follows this structural regime with explicit parity labels. 

C. Kernel-Target Alignment 

Kernel-target alignment (KTA), introduced by Cristianini et al. [5], measures how well a kernel matrix aligns with the ideal kernel derived from labels. KTA values close to 1 indicate strong alignment, while values near 0 suggest the kernel is uninformative for the task. Recent work has used KTA for quantum kernel design and evaluation [6], showing that high KTA correlates with better classification performance. III. B ACKGROUND 

A. Kernel Methods and Support Vector Machines 

Kernel methods implicitly map data to a feature space F

via K(x, y ) = ⟨ϕ(x), ϕ (y)⟩; SVMs find a maximum-margin hyperplane in F. The RBF kernel K(x, y ) = exp( −γ∥x−y∥2)

measures smooth Euclidean similarity, which breaks down for parity functions: flipping a single bit changes the label while barely changing Euclidean distance. Polynomial kernels 

K(x, y ) = ( x⊤y + c)d can represent d-th order interactions but, as our degree-11 experiment shows, fail in practice on 11-way parity at realistic sample sizes. 

B. Quantum Kernels 

A quantum kernel uses a parameterized quantum circuit 

Uϕ(x) to map classical data x to a quantum state |ϕ(x)⟩ =

Uϕ(x)|0⟩⊗n. The kernel function is defined as: 

Kq (x, y ) = |⟨ ϕ(x)|ϕ(y)⟩| 2 (1) This quantum kernel can replace classical kernels in SVMs by precomputing the kernel matrix (Gram matrix) for training and test data. 

C. ZZ Feature Map 

The ZZ feature map consists of alternating layers of single-qubit rotations and two-qubit entangling gates. For a data vector 

x ∈ Rn and r repetitions, the circuit is: 

Uϕ(x) = [ UZ (x) · UZZ (x)] r (2) where: 

UZ (x) = 

> n

O

> i=1

eix iZi (3) 

UZZ (x) = Y

> (i,j )

ei(π−xi)( π−xj )ZiZj (4) The ZiZj terms create entanglement that depends on the product of features xi and xj , naturally encoding pairwise correlations. With full entanglement and r repetitions, this map can represent multivariate polynomials up to degree 2r.

D. Parity-Structured Benchmark Structure 

Our synthetic benchmark places samples on vertices of an n-dimensional hypercube defined by informative features 

{x1, . . . , x n}. Labels are assigned via: 

y =

> n

M

> i=1

⊮[xi > median (xi)] = 

> n

X

> i=1

bi mod 2 (5) where bi ∈ {0, 1} is the binary threshold indicator. This 

n-way XOR structure has 2n distinct input configurations, partitioned into two equal-sized parity classes. No subset of fewer than n features is sufficient for perfect classification, and the decision boundary forms a non-smooth, multi-connected surface in feature space. As n grows, the problem becomes combinatorially harder: a Hamming-based classical kernel must distinguish among 2n configurations using only pairwise feature agreements, while a quantum kernel operates natively in a 2n-dimensional Hilbert space. IV. M ETHOD 

A. Problem Formulation 

We consider binary classification with parity-structured labels. Given n samples {(xi, y i)}ni=1 where xi ∈ Rd and 

yi ∈ { 0, 1}, the label is determined by: 

yi = parity (bi1, b i2, . . . , b ik ) (6) where bij = ⊮[xij > θ j ] and θj is a threshold (typically the median). Our goal is to learn a classifier f : Rd → { 0, 1} that generalizes to unseen test data. B. Data Pipeline 1) Dataset Generation: We generate synthetic parity-structured data using scikit-learn’s make_classification ,treating the number of informative features n as a variable parameter swept in our experiments. The fixed parameters are:  

> •

800 samples, 500 features (560 train / 240 test, stratified 70/30 split)  

> •

n informative features (defining the n-way XOR parity; swept over n ∈ { 5, . . . , 11 }) 

> •

20 redundant features (linear combinations of informative features)  

> •

Remainder: probe features (random noise)  

> •

16 clusters per class (32 total) on hypercube vertices; fixed across all n 

> •

Class separation: 0.25  

> •

Label noise: 22% ( flip_y = 0 .22 ) for main results; varied in sweep After generation, we replace the geometric labels with n-way parity labels computed by median-thresholding the n

informative features, then apply 22% random label flipping to simulate realistic noise. The 800-sample budget is chosen to keep exact statevector simulation tractable at 11 qubits across 10 random seeds. 

2) Dimensionality Reduction: Parity functions carry zero marginal information per individual feature —each feature is uncorrelated with the label on its own; only their conjunction matters. Consequently, greedy elimination methods such as RFE fail to identify the informative set: across 10 seeds, RFE with Random Forest recovers only 0.4 ± 0.7 of the 11 true features (essentially selecting at random from 500 dimensions). For this synthetic benchmark the informative features are known by construction (the first n columns of 

make_classification ), and we select them directly. In real-world settings, this finding underscores the need for feature selection methods sensitive to joint, not marginal, feature interactions. 

3) Feature Encoding: This is where our approach diverges significantly from standard practice: 

Classical methods use continuous features scaled to [0 , 2π]

via min-max normalization: 

xclassical  

> i

= xi − min( xi)max( xi) − min( xi) · 2π (7) 

Quantum method uses binary encoding that explicitly exposes parity structure: 

xquantum  

> i

=

(

π if xi > median (xi)0 otherwise (8) This binary encoding transforms the continuous feature space into a discrete representation aligned with the parity computation. Since quantum circuits use rotation angles and the ZZ feature map computes products like (π − xi)( π − xj ),the binary encoding creates maximal phase differences between parity-flipped states. This is analogous to basis encoding in quantum computing, where discrete states are naturally represented as distinct quantum phases. 

4) Train-Test Split: We use a stratified 70%–30% train-test split (560 train / 240 test at 800 samples). For statistical rigor, we report results over 10 random seeds (0–9) and give mean 

± standard deviation. 

C. Classical Baseline Methods 

We evaluate multiple classical methods (all using the continuous scaled features) to probe the classical limit: 1) Linear SVC : Expected to fail ( ∼50% accuracy) as parity is not linearly separable. 2) RBF SVC (continuous) : Grid search over C and γ with 5-fold cross-validation on scaled continuous features. 3) RBF SVC (binary) : Same RBF SVM training protocol applied to the binary {0, π } features used by the quantum kernel. This ablation controls for preprocessing: if binary encoding alone drives the gain, this baseline should match or exceed the quantum kernel’s accuracy. 4) Random Forest : Grid search over nestimators , max depth ,and min samples split. 5) XGBoost : Gradient boosted trees with grid search over 

nestimators , max depth, and learning rate. 6) MLP : Multi-layer perceptron with grid search over hidden layer sizes and regularization. 7) Poly SVC (binary, d = 11 ): Polynomial SVM of degree 11—matching the XOR order—applied to the same {0, π } features as the quantum kernel. Grid search over C ∈ { 0.1, 1, 10 , 100 , 1000 } and offset. If a classical polynomial kernel of sufficient degree explains the gain, this baseline should approach quantum accuracy. 

D. Quantum Kernel Method 

Our quantum pipeline consists of: 1) Feature Map : ZZ feature map with r = 3 repetitions and full entanglement, operating on n qubits (one per selected informative feature). For main results, n = 11 (Hilbert space dimension 211 = 2048 ). With r = 3 repetitions, the ZZ map can represent multivariate interactions up to degree 6. 2) Kernel Computation : Exact statevector simulation using Qiskit’s FidelityStatevectorKernel to compute: 

Ktrain [i, j ] = |⟨ ϕ(xtrain  

> i

)|ϕ(xtrain  

> j

)⟩| 2 (9) 

Ktest [i, j ] = |⟨ ϕ(xtest  

> i

)|ϕ(xtrain  

> j

)⟩| 2 (10) 3) Classification : SVM with precomputed kernel. We tune C ∈ { 1, 10 , 100 , 1000 , 10000 } via 5-fold cross-validation on the training set. The quantum feature map uses the binary-encoded features 

xquantum ∈ { 0, π }n, making each quantum state correspond to a vertex of an n-dimensional binary hypercube in the computational basis. The choice r = 3 (rather than r = 5 )was identified via our complexity sweep: at high noise and high n, fewer repetitions are more robust, consistent with the observation that overly deep circuits can overfit to noisy labels. TABLE I COMPLEXITY SWEEP : QUANTUM ZZ VS . RBF SVC ( BINARY ) AT FIXED NOISE 30%, r = 3 . G AP > 0 MEANS QUANTUM WINS . T RANSITION OCCURS BETWEEN n = 8 AND n = 9 .                        

> nQuantum ZZ RBF (binary) Gap KTA (Q) 70.594 0.644 −0.050 0.021
> 80.567 0.607 −0.040 0.039
> 90.580 0.577 +0 .003 0.045
> 10 0.593 0.522 +0 .071 0.060
> 11 0.599 0.533 +0 .066 0.062

E. Evaluation Metrics 

We assess performance using: 1) Test Accuracy : Fraction of correctly classified test samples. 2) Kernel-Target Alignment (KTA) : Measures kernel-label correlation: KTA (K, y ) = ⟨K, yy ⊤⟩F

∥K∥F ∥yy ⊤∥F

(11) where ⟨· , ·⟩ F is the Frobenius inner product. V. E XPERIMENTS 

A. Experimental Setup 

All experiments were conducted on a workstation with the following configuration:  

> •

Software : Python 3.11, Qiskit 1.0, Qiskit Machine Learn-ing 0.7, scikit-learn 1.4  

> •

Simulation : Exact statevector simulation (no shot noise)  

> •

Dataset : 800 samples (560/240 train/test), n = 11 for main results; 10 random seeds (0–9)  

> •

Quantum Circuit : 11 qubits, r = 3 ZZ layers, full entanglement; Hilbert space 211 = 2048  

> •

Complexity sweep : n ∈ {5, . . . , 11 }, noise ∈{0.15 , . . . , 0.30 }; classical phase run first (2000 samples), quantum on top candidates (800 samples) 

B. Complexity Sweep: Identifying the Quantum Advantage Threshold 

A central question is: at what parity complexity n does the quantum kernel begin to outperform classical methods that also receive binary {0, π } features? To answer this, we sweep n ∈ { 7, 8, 9, 10 , 11 } at fixed label noise 30% and r = 3 

ZZ repetitions, comparing the quantum ZZ kernel against the binary RBF ablation. Table I shows the results (5 seeds each; classical at 2000 samples, quantum at 800 samples). The data reveal a clear phase transition: for n ≤ 8,the binary RBF outperforms the quantum kernel (Hamming distance is sufficient for low-complexity XOR); for n ≥ 9,the quantum kernel wins and the gap grows with n. This is consistent with the theoretical intuition that the quantum Hilbert space ( 2n dimensions) becomes increasingly expressive relative to classical Hamming-based kernels as n grows. The KTA for the quantum kernel also increases monotonically with                                                    

> TABLE II TEST ACCURACY (MEAN ±STD , 10 SEEDS ), n= 11 ,F L I P _Y= 0 .22 , 800
> SAMPLES .†BINARY {0, π }FEATURES ;‡POLY SVM DEGREE 11 ON SAME
> {0, π }FEATURES . A LL CLASSICAL METHODS COLLAPSE TO CHANCE ;ONLY THE QUANTUM KERNEL BREAKS THE PARITY BARRIER .
> Method Test Accuracy
> Linear SVC 0.502 ±0.017
> RBF SVC (continuous) 0.500 ±0.020
> Random Forest 0.496 ±0.024
> XGBoost 0.501 ±0.019
> MLP 0.498 ±0.022
> RBF SVC (binary) †0.543 ±0.011
> Poly SVC (binary, d= 11 )‡0.500 ±0.045
> Quantum ZZ Kernel (n= 11 ,r= 3 )0.663 ±0.032

n, confirming improved geometric alignment with the label structure. At the optimal noise level for n = 11 (flip_y = 0 .22 ), the gap widens to +12 .0 pp over 10 seeds, which we report as our main result. 

C. Classification Results 

Table II summarizes test accuracy (mean ± std over 10 seeds) for all methods at n = 11 , flip_y = 0 .22 . At 11-way XOR parity, all continuous methods score near 50% (random chance): neither Euclidean distance nor tree-based splitting can capture 11-way discrete correlations. The binary RBF ablation—given the exact same {0, π } features as the quantum kernel—reaches only 54 .3% ±1.1% , barely above random. The quantum ZZ kernel breaks out to 66 .3% ± 3.2%, a margin of 

+12 .0 percentage points over the binary ablation. This confirms that the gain is driven by the quantum circuit’s ability to encode 11-way correlations in its 211 -dimensional Hilbert space, not by the binary encoding alone. Fig. 1 visualizes these results. A polynomial SVM of degree 11 applied to the same binary {0, π } features—theoretically capable of representing 11-way XOR—scores only 50 .0% ±

4.5% , indistinguishable from chance. This confirms that having the correct theoretical capacity does not suffice in practice: the SVM cannot extract the 11-way parity pattern from 560 training samples at 22% noise using a polynomial expansion. The quantum ZZ kernel achieves 66 .3% ± 3.2%, a +16 .3

pp margin over the degree-11 polynomial and +12 .0 pp over the binary RBF ablation, demonstrating that the advantage is specific to the quantum circuit’s Hilbert-space structure. 

D. Kernel-Target Alignment 

Table III shows kernel-target alignment (mean ± std over 10 seeds) at n = 11 . The quantum kernel exhibits ∼7× higher KTA than RBF, indicating much stronger geometric alignment with the 11-way parity task structure. Fig. 2 visualizes this comparison. The low KTA for RBF (0.013 ) reflects its poor alignment with 11-way parity structure, while the quantum kernel’s higher KTA ( 0.094 ) demonstrates that its 211 -dimensional Hilbert space aligns substantially better with the task geometry. Fig. 1. Test accuracy (mean ± std over 10 seeds, n = 11 , flip_y = 0 .22 ). The quantum ZZ kernel substantially outperforms all classical baselines; all five continuous methods collapse to near-random, confirming that the advantage is structural rather than incidental. TABLE III KERNEL -T ARGET ALIGNMENT (KTA, MEAN ± STD OVER 10 SEEDS ,        

> n= 11 )
> Kernel KTA
> RBF 0.013 ±0.001
> Quantum ZZ 0.094 ±0.020

VI. D ISCUSSION 

A. Why Does the Quantum Kernel Win? 

The performance gap stems from the synergy between the ZZ feature map and binary encoding: 1) Parity-Aligned Representation : The binary encoding 

{0, π } directly reflects the thresholding operation in parity computation, making the quantum states explicitly encode the binary hypercube structure. 2) ZZ Entanglement : The ZiZj gates create quantum interference patterns that depend on products xixj . For binary xi ∈ { 0, π }, these products create maximal phase separations between parity-flipped configurations. 3) High-Order Correlations : With r = 3 repetitions, the ZZ map can represent multivariate correlations up to degree 6. While this is less than the 11-way XOR order, the exponential Hilbert space ( 211 = 2048 dimensions) provides sufficient capacity to capture the combinatorial structure through interference patterns that no polynomial-degree argument fully captures. In contrast, the RBF kernel measures Euclidean distance, which treats all bit-flips equally and cannot distinguish parity-preserving from parity-flipping perturbations. Tree-based meth-ods (Random Forest, XGBoost) decompose the feature space by individual feature thresholds; at n = 11 , the XOR rule requires jointly considering all 11 features simultaneously, which is exponentially hard for greedy tree splits.      

> Fig. 2. Kernel–target alignment comparison ( n= 11 ). The quantum ZZ kernel shows ∼7×higher alignment with labels compared to RBF ( 0.094 vs.
> 0.013 ), consistent with the accuracy gap.

B. Parity Complexity as the Key Axis 

Our complexity sweep (Table I) reveals a clear threshold effect. At n ≤ 8, the binary RBF ablation outperforms the quantum kernel: encoding alone (Hamming similarity on {0, π }

features) is sufficient. At n = 5 under lower noise, binary RBF achieves 83 .4% vs. quantum’s 81 .2% —a regime where encoding dominates. As n increases, classical Hamming-based similarity becomes a weaker proxy for n-way XOR: the number of distinct parity-class configurations grows as 2n, but Hamming distance only scales linearly. The quantum kernel crosses over at n ≈ 9 and maintains a growing advantage. At 

n = 11 (10 seeds, flip_y = 0 .22 ), binary RBF reaches only 

54 .3% while the quantum kernel achieves 66 .3% , because:  

> •

The quantum Hilbert space grows exponentially ( 2n =2048 at n = 11 ), preserving discriminative structure even as the parity landscape becomes more complex.  

> •

The ZZ feature map’s ZiZj interference encodes all pairwise bit correlations simultaneously, efficiently rep-resenting the XOR’s combinatorial structure at higher 

n. 

> •

Binary encoding ({0, π }) remains the correct representation—the key differentiator at high n is the quantum circuit’s expressivity, not just the encoding. 

C. Binary vs. Continuous Encoding: Ablation 

At n = 11 , the binary RBF ablation scores 54 .3% ± 1.1% —marginally above the continuous RBF ( 50 .0% ) but far below the quantum kernel ( 66 .3% ). Binary encoding provides a small lift (roughly 4 pp) that is consistent across seeds (low standard deviation), but the quantum circuit’s exponential Hilbert space captures the remaining 11-way parity correlations that Hamming similarity cannot. The 12 .0 pp gap between the binary ablation and the quantum kernel unambiguously attributes the advantage to the quantum feature map rather than preprocessing. D. Limitations 

1) Simulation Only : All experiments used exact statevector simulation. Real quantum hardware introduces shot noise, gate errors, and decoherence, which may degrade performance. 2) Synthetic Data : We used a synthetic parity-structured dataset generated via scikit-learn. The original UCI Madelon dataset has different label structure; validation on that or other real benchmarks is left to future work. 3) Gaussian Process Omitted : Gaussian process classifiers were not evaluated due to O(n3) cost on 2000 samples; RBF SVM serves as a representative kernel baseline. 4) Scalability : We demonstrated the approach up to 11 qubits via exact statevector simulation. Scaling to 20+ qubits would require real quantum hardware or approxi-mate simulation. Additionally, quantum kernels can suffer from exponential concentration [10] as qubit count grows, which may limit their practical utility at larger scales. VII. C ONCLUSION 

We have demonstrated that quantum kernel advantage on parity-structured classification is governed by parity com-plexity —the number of features entering the XOR rule. Our systematic sweep of n ∈ { 7, . . . , 11 } (Table I) reveals a phase transition around n ≈ 9: below this threshold, binary encoding alone (RBF SVM on {0, π } features) suffices; above it, the quantum ZZ kernel provides a growing advantage that reaches 

+12 .0 pp at n = 11 . At n = 11 (10 seeds, 800 samples, r = 3 ): quantum ZZ reaches 66 .3% ± 3.2% versus 54 .3% ± 1.1% for the binary RBF ablation and ∼50% for all five continuous baselines, with ∼7× higher kernel–target alignment ( 0.094 vs. 

0.013 ). Crucially, the binary ablation—trained on the same 

{0, π } features as the quantum kernel—does not close the gap, confirming that the quantum circuit’s exponential 211 -dimensional Hilbert space, not the encoding, is the source of advantage. These results provide empirical evidence that, for a parity-structured synthetic benchmark, the proposed quantum kernel model can outperform standard classical baselines. We do not interpret this as a general claim of “quantum advantage” in the complexity-theoretic sense [9]; rather, it highlights the importance of representation design in quantum machine learning [6]—matching circuit structure and encoding to the problem’s combinatorial symmetry. The binary encoding scheme introduced here is tailored to quantum circuits and parity structure, offering a principled approach for encoding discrete classification problems. This is a proof of concept on synthetic data; the combinatorial structure studied here arises naturally in epistatic genomics (SNP combinations determining disease phenotype [7]), drug combination therapy, and digital circuit fault analysis—domains where binary features interact non-additively and smooth kernels are known to struggle. The advantage reported here is robust to 22% label noise; gate and decoherence noise on real quantum hardware remains the key open question for deployment. Future work should explore: (1) validation on real-world benchmarks with latent parity-like structure, (2) implementation on NISQ devices [11] with error mitigation, (3) theoretical analysis of when quantum kernels outperform classical coun-terparts [8], and (4) investigation of concentration phenomena [10] that may limit expressivity at larger qubit counts. ACKNOWLEDGMENT 

The authors thank the reviewers for their constructive feedback; in particular, the suggestion to evaluate a classical RBF baseline on the same binary {0, π } features substantially strengthened the empirical contribution by cleanly separating encoding effects from circuit effects. The authors also thank the open-source community for Qiskit and scikit-learn, which made this research possible. REFERENCES [1] I. Guyon, S. Gunn, A. Ben-Hur, and G. Dror, “Result analysis of the NIPS 2003 feature selection challenge,” in Advances in Neural Information Processing Systems , vol. 17, 2003. [2] I. Guyon, S. Gunn, A. Ben-Hur, and G. Dror, “Design and analysis of the NIPS2003 challenge,” in Feature Extraction , Springer, 2004, pp. 237–263. [3] V. Havl ´ı ˇcek, A. D. C ´orcoles, K. Temme, A. W. Harrow, A. Kandala, J. M. Chow, and J. M. Gambetta, “Supervised learning with quantum-enhanced feature spaces,” Nature , vol. 567, no. 7747, pp. 209–212, 2019. [4] M. Schuld and N. Killoran, “Quantum machine learning in feature Hilbert spaces,” Physical Review Letters , vol. 122, no. 4, p. 040504, 2019. [5] N. Cristianini, J. Shawe-Taylor, A. Elisseeff, and J. Kandola, “On kernel-target alignment,” in Advances in Neural Information Processing Systems ,vol. 14, 2002. [6] M. Schuld, “Supervised quantum machine learning models are kernel methods,” arXiv preprint arXiv:2101.11020, 2021. [7] J. Biamonte, P. Wittek, N. Pancotti, P. Rebentrost, N. Wiebe, and S. Lloyd, “Quantum machine learning,” Nature , vol. 549, pp. 195–202, 2017. [8] Y. Liu, S. Arunachalam, and K. Temme, “A rigorous and robust quantum speed-up in supervised machine learning,” Nature Physics , vol. 17, pp. 1013–1017, 2021. [9] H.-Y. Huang, M. Broughton, M. Mohseni, R. Babbush, S. Boixo, H. Neven, and J. R. McClean, “Power of data in quantum machine learning,”