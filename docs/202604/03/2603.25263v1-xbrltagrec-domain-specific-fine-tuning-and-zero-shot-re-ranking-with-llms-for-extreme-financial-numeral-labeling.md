---
title: "XBRLTagRec: Domain-Specific Fine-Tuning and Zero-Shot Re-Ranking with LLMs for Extreme Financial Numeral Labeling"
title_zh: XBRLTagRec：基于大语言模型的领域特定微调与零样本重排序，用于极端金融数值标注
authors: "Gang Hu, Qun Zhang, Jingyao Luo, Yile Jiang, Jing Chai, Haiyan Ding"
date: 2026-03-26
pdf: "https://arxiv.org/pdf/2603.25263v1"
tags: ["query:q14"]
score: 9.0
evidence: 使用大语言模型进行自动化财务数值标注和XBRL标签选择
tldr: 针对上市公司XBRL财务报告中标签选择困难的问题，本文提出XBRLTagRec框架。该框架结合了领域特定微调与大模型重排序技术，通过FLAN-T5-Large生成语义文档并利用ChatGPT-3.5进行零样本重排序，有效解决了极大规模分类体系下相似标签难以区分的挑战，在FNXL数据集上显著提升了匹配准确率。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.25263v1/fig-001.webp\", \"caption\": \"Fig. 2. Our proposed XBRLTagRec framework consists of three components: (A) Tag Document Generation, (B) Semantic Similarity Retrieval, and (C) LLM Zero-Shot Re-Ranking. First, a FLAN-T5 LoRA-tuned LLM generates a tag document from instruction prompts, financial text, and related numeric questions. Next, the Sentence-T5-XXL model encodes this document and all ground-truth XBRL documents to retrieve the Top-10 most similar ones via cosine similarity. Finally, ChatGPT-3.5 performs multi-round re-ranking of these candidates, and the final XBRL tags are selected by majority vote.\", \"page\": 2, \"index\": 1, \"width\": 1062, \"height\": 306}, {\"url\": \"assets/figures/arxiv/2603.25263v1/fig-002.webp\", \"caption\": \"TABLE II THE IMPACT OF ITERATION RANGE ON XBRLTAGREC’S RE-RANKING MODULE. THE BEST RESULT IS SHOWN IN BOLD.\", \"page\": 5, \"index\": 2, \"width\": 506, \"height\": 271}]"
motivation: 现有的XBRL标签自动标注方法在面对大规模且语义高度相似的财务标签分类体系时，难以实现精准区分。
method: 提出XBRLTagRec框架，利用微调的FLAN-T5-Large生成语义标签文档进行初步检索，并结合ChatGPT-3.5进行零样本重排序。
result: "在FNXL数据集上的实验表明，该方法在Hits@1和Macro指标上比现有SOTA模型FLAN-FinXC提升了2.64%至4.47%。"
conclusion: 结合领域微调与大模型重排序的端到端框架能有效处理大规模且复杂的财务数据标签匹配任务。
---

## 摘要
上市公司必须根据美国证券交易委员会（SEC）的规定和公认会计原则（GAAP）披露财务信息。可扩展商业报告语言（XBRL）作为一种基于 XML 的财务语言，实现了标准化和机器可读的报告，但从庞大的分类标准中准确选择标签仍然具有挑战性。现有的基于微调的方法难以区分高度相似的 XBRL 标签，限制了其在财务数据匹配中的性能。为解决这些问题，我们提出了 XBRLTagRec，这是一个用于自动化金融数值标注的端到端框架。该框架利用微调后的 FLAN-T5-Large 模型生成语义标签文档，通过语义相似度检索相关候选标签，并应用 ChatGPT-3.5 进行零样本重排序以选择最优标签。在 FNXL 数据集上的实验表明，XBRLTagRec 的表现优于目前最先进的 FLAN-FinXC 框架，在 Hits@1 和 Macro 指标上实现了 2.64%-4.47% 的提升。这些结果证明了其在大规模且语义复杂的标签匹配场景中的有效性。

## Abstract
Publicly traded companies must disclose financial information under regulations of the Securities and Exchange Commission (SEC) and the Generally Accepted Accounting Principles (GAAP). The eXtensible Business Reporting Language (XBRL), as an XML-based financial language, enables standardized and machine-readable reporting, but accurate tag selection from large taxonomies remains challenging. Existing fine-tuning-based methods struggle to distinguish highly similar XBRL tags, limiting performance in financial data matching. To address these issues, we introduce XBRLTagRec, an end-to-end framework for automated financial numeral tagging. The framework generates semantic tag documents with a fine-tuned FLAN-T5-Large model, retrieves relevant candidates via semantic similarity, and applies zero-shot re-ranking with ChatGPT-3.5 to select the optimal tag. Experiments on the FNXL dataset show that XBRLTagRec outperforms the state-of-the-art FLAN-FinXC framework, achieving 2.64%-4.47% improvements in Hits@1 and Macro metrics. These results demonstrate its effectiveness in large-scale and semantically complex tag matching scenarios.

---

## 论文详细总结（自动生成）

### 论文总结：XBRLTagRec —— 基于大语言模型的极端金融数值标注框架

#### 1. 核心问题与整体含义（研究动机和背景）
在金融监管（如 SEC 和 GAAP）要求下，上市公司需使用 **XBRL（可扩展商业报告语言）** 对财务报告进行标准化标注。然而，XBRL 标签体系极其庞大且复杂，存在严重的**语义重叠**（标签定义高度相似）和**极端多标签分类**挑战。现有的自动化方法（如基于 NER 或简单分类的模型）在区分细微语义差异时表现不佳，且难以处理长尾标签。本文旨在通过结合领域特定微调与大语言模型（LLM）的推理能力，提高财务数值标签匹配的准确性与可解释性。

#### 2. 方法论：核心思想与技术流程
论文提出了 **XBRLTagRec** 框架，这是一个包含三个阶段的端到端流程：
*   **标签文档生成 (Tag Document Generation)**：使用 **FLAN-T5-Large** 模型作为核心生成器，通过 **LoRA (Low-Rank Adaptation)** 进行参数高效微调。模型输入财务文本、指令和目标数值问题，输出一段描述该数值语义的“标签文档”，而非简单的标签名称，以捕捉更丰富的语义结构。
*   **语义相似度检索 (Semantic Similarity Retrieval)**：利用预训练的 **Sentence-T5-XXL** 模型将生成的标签文档和所有标准 XBRL 标签文档转化为向量，通过**余弦相似度**检索出最相关的 **Top-10** 候选标签。
*   **LLM 零样本重排序 (Zero-Shot Re-Ranking)**：引入 **ChatGPT-3.5** 作为重排序器。
    *   **过滤策略**：将 Top-10 候选随机分为两组（每组 5 个），让 LLM 分别选出每组中最匹配的候选。
    *   **多轮迭代与投票**：通过多次随机分组和重排序（1-13 轮），统计各候选标签被选为第一名的频率，最终通过**多数投票 (Majority Vote)** 确定最优标签。

#### 3. 实验设计
*   **数据集**：使用 **FNXL 数据集**的公开子集。该数据集源自 SEC 的 10-K 文件，包含 2,339 家公司、约 14.3 万个标注实例，涉及 2,794 个不同的 XBRL 标签，具有明显的长尾分布特征。
*   **Benchmark 与对比方法**：
    *   **FLAN-FinXC** (当前最先进的生成式 Baseline)。
    *   **AttentionXML** (基于 BERT 的极端多标签分类)。
    *   **FiNER** (基于 NER 的序列标注)。
    *   **GalaXC** (基于图神经网络的方法)。
    *   **Label Semantics** (结合标签语义描述的方法)。
*   **评价指标**：Hits@1、Macro-Precision (M-P)、Macro-Recall (M-R) 和 Macro-F1 (M-F1)。

#### 4. 资源与算力
*   **硬件**：使用了一台 **NVIDIA Tesla V100 (32GB)** GPU。
*   **模型配置**：FLAN-T5-Large 和 Sentence-T5-XXL 的预训练权重来自 Hugging Face。
*   **微调细节**：LoRA rank=2，alpha=32，学习率 5e-4，训练 5 个 epoch，batch size 为 8。
*   **API 调用**：重排序阶段使用了 `gpt-3.5-turbo-1106` 接口。

#### 5. 实验数量与充分性
论文进行了较为充分的实验验证：
*   **主实验**：在 FNXL 数据集上与 5 种主流基准模型进行对比。
*   **参数敏感性实验**：探讨了迭代轮数（1-13 轮）、分组大小（3-7 个）以及分组策略（保持顺序 vs. 随机打乱）对性能的影响。
*   **跨模型泛化实验**：将重排序器更换为 **GPT-4o、DeepSeek-V3、ERNIE-3.5** 等不同 LLM，验证框架的稳健性。
*   **客观性**：实验涵盖了从分类到生成的多种技术路线，对比指标全面，能够客观反映模型在处理不平衡数据和复杂语义时的能力。

#### 6. 主要结论与发现
*   **性能领先**：XBRLTagRec 在所有指标上均优于 SOTA 模型 FLAN-FinXC。在 1-8 轮迭代下，Hits@1 提升了 **2.64%**，Macro-F1 提升了 **4.34%**。
*   **重排序的价值**：单纯依靠语义检索（Top-1）可能遗漏正确标签，而 LLM 的零样本重排序能有效识别极其相似标签之间的细微差别。
*   **参数影响**：迭代 8 轮左右性能达到饱和，分组大小为 5 且保持原始检索顺序（Order-Preserving）时效果最佳。
*   **模型通用性**：该框架具有“插拔式”特征，更换不同的 LLM 后依然能保持优于基准线的性能，其中 GPT-4 表现最强。

#### 7. 优点
*   **端到端闭环**：结合了生成、检索和重排序，充分利用了 LLM 的生成能力和逻辑推理能力。
*   **模块化设计**：生成器、编码器和重排序器均可灵活更换，具有很强的工程适应性和扩展性。
*   **鲁棒性强**：通过多轮迭代和频率投票机制，降低了 LLM 在单次推理中可能产生的随机偏差。

#### 8. 不足与局限
*   **成本问题**：多轮调用 ChatGPT API 会显著增加计算成本和推理延迟，在大规模实时标注场景下可能受限。
*   **嵌入模型依赖**：论文未深入探讨不同 Embedding 模型（如专门的金融领域嵌入模型）对第一阶段检索效果的影响。
*   **零样本局限**：未研究在完全不进行微调的情况下，通用 LLM 处理此类极端数值标注任务的极限能力。

（完）
