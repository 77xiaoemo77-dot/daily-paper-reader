---
title: Forecast collapse of transformer-based models under squared loss in financial time series
title_zh: 金融时间序列中基于 Transformer 的模型在平方损失下的预测崩溃
authors: Pierre Andreoletti
date: 2026-03-31
pdf: "https://arxiv.org/pdf/2604.00064v1"
tags: ["query:q16"]
score: 9.0
evidence: 基于Transformer模型的金融时间序列预测
tldr: 本研究探讨了Transformer模型在处理弱条件结构的金融时间序列时，为何在平方损失下会出现预测崩溃。研究发现，在条件期望趋于退化的场景中，高表达性模型无法提升精度，反而会因重复利用噪声产生伪轨迹波动，导致预测方差显著增加。通过高频汇率数据的实验验证，证明了Transformer在多数预测窗口下的表现均逊于简单的线性基准，揭示了方差驱动的性能退化机制。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.00064v1/fig-001.webp\", \"caption\": \"Figure 1: Distribution of trajectory forecasting errors on EUR/USD 30-second data. Panel (a): empirical cumulative distribution functions. Panel (b): distribution of the log-ratio of errors.\", \"page\": 14, \"index\": 1, \"width\": 720, \"height\": 300}]"
motivation: 旨在解释为何高表达能力的Transformer模型在金融时间序列预测中往往表现不佳，甚至不如简单的线性模型。
method: 通过平方损失下的风险最小化理论，分析弱条件结构序列中模型表达能力对预测偏差和方差的影响。
result: 实验显示Transformer模型在EUR/USD高频数据上的预测误差普遍高于线性基准，且伴随大量的伪轨迹波动。
conclusion: 在金融等低信噪比场景下，提升模型复杂度会因噪声复用导致预测方差激增，从而引发预测性能的崩溃。
---

## 摘要
我们研究了在具有弱条件结构的时间序列中，使用高表达能力预测模型在平方损失下的轨迹预测。基于平方损失风险最小化的经典表征，我们重点探讨了未来轨迹的条件期望实际上发生退化的情形，这导致了平凡的贝叶斯最优预测器（在标准金融设定下，价格预测表现为平直曲线，收益率预测则为零）。在这种情形下，增加模型的表达能力并不能提高预测准确性，反而会在最优预测器周围引入伪轨迹波动。这些波动源于对噪声的重复利用，导致预测方差增加，而偏差并未减少。这为基于 Transformer 的预测在金融时间序列上性能退化提供了一个过程层面的解释。我们通过对高频欧元/美元（EUR/USD）汇率数据的数值实验补充了这些理论结果，并分析了轨迹级预测误差的分布。结果表明，在绝大多数预测窗口中，基于 Transformer 的模型产生的误差均大于简单的线性基准模型，这与理论所识别的方差驱动机制相一致。

## Abstract
We study trajectory forecasting under squared loss for time series with weak conditional structure, using highly expressive prediction models. Building on the classical characterization of squared-loss risk minimization, we emphasize regimes in which the conditional expectation of future trajectories is effectively degenerate, leading to trivial Bayes-optimal predictors (flat for prices and zero for returns in standard financial settings).  In this regime, increased model expressivity does not improve predictive accuracy but instead introduces spurious trajectory fluctuations around the optimal predictor. These fluctuations arise from the reuse of noise and result in increased prediction variance without any reduction in bias.  This provides a process-level explanation for the degradation of Transformerbased forecasts on financial time series.  We complement these theoretical results with numerical experiments on high-frequency EUR/USD exchange rate data, analyzing the distribution of trajectory-level forecasting errors. The results show that Transformer-based models yield larger errors than a simple linear benchmark on a large majority of forecasting windows, consistent with the variance-driven mechanism identified by the theory.

---

## 论文详细总结（自动生成）

### 论文详细总结：金融时间序列中 Transformer 模型的预测崩溃分析

#### 1. 核心问题与整体含义（研究动机和背景）
本文探讨了为什么在电力、交通等领域表现优异的 **Transformer 架构**，在应用于**金融时间序列**（如股票价格或汇率）时长程预测效果极差，甚至会出现“预测崩溃”现象（即价格预测趋于平直线，收益率预测趋于零）。
*   **研究动机**：现有解释多归因于低信噪比、非平稳性或数据稀缺。作者认为这些解释是启发式的，并未触及本质。
*   **核心观点**：在金融这种“弱条件结构”序列中，平方损失（MSE）下的贝叶斯最优预测器本身就是平凡的（Trivial）。此时，增加模型表达能力（Expressivity）不仅不能挖掘潜在规律，反而会因为“噪声复用”产生伪波动，导致预测方差剧增，性能反而不如简单线性模型。

#### 2. 方法论：核心思想与关键技术细节
论文从统计学习理论出发，对比了简单线性模型与高表达性模型在轨迹预测中的表现：
*   **贝叶斯最优预测器**：证明在平方损失下，最优预测器是未来轨迹相对于过去观测的**条件期望**。在金融市场（近似马尔可夫或鞅过程）中，这个期望值对于价格就是当前值，对于收益率就是零。
*   **噪声复用机制（Noise Reuse）**：高表达性模型（如 Transformer）在训练时倾向于拟合训练集中的随机噪声。在预测阶段，模型会将这些历史噪声“重新注入”到预测轨迹中，产生看似复杂但毫无预测力的波动。
*   **理论推导**：作者通过数学证明显示，在弱信号制度下，插值型预测器（如近邻法或过拟合的神经网络）的预期误差至少是噪声方差的两倍（$2H\sigma^2$），而简单的线性回归器误差仅略高于噪声方差（$H\sigma^2 + O(1/n)$）。

#### 3. 实验设计
*   **数据集**：使用高频 **EUR/USD（欧元/美元）汇率数据**，采样频率为 30 秒，涵盖约 1160 个交易日。
*   **实验场景**：回顾窗口（Lookback）$L=451$ 个观测值，预测窗口（Horizon）$H=30$ 个步长。
*   **对比方法（Benchmark）**：
    1.  **PatchTST**：一种先进的基于 Patch 的 Transformer 预测模型，代表高表达性模型。
    2.  **线性基准（Linear Benchmark）**：一个简单的单参数线性回归模型。
*   **评估指标**：轨迹级平方损失的分布、经验累积分布函数（ECDF）、以及两种模型误差的对数比率。

#### 4. 资源与算力
*   论文**未明确说明**具体的硬件配置（如 GPU 型号、数量）或具体的训练时长。
*   考虑到实验涉及的是单变量高频汇率预测且样本量约为 1159 个窗口，预计算力需求在中等规模工作站即可满足。

#### 5. 实验数量与充分性
*   **实验规模**：使用了 70% 训练、10% 验证、20% 测试的划分比例，测试集包含约 233 个独立的预测窗口。
*   **充分性评价**：
    *   **客观性**：实验结果不仅展示了平均误差，还展示了误差的完整分布。结果显示 PatchTST 在 **92%** 的测试窗口中表现均劣于线性模型，证明了结论的普适性而非偶然。
    *   **局限性**：实验仅针对单一资产（EUR/USD）和单一先进模型（PatchTST），虽然具有代表性，但若能涵盖更多资产类别（如股票、商品）或更多 Transformer 变体（如 Informer, Autoformer）会更具说服力。

#### 6 主要结论与发现
*   **预测崩溃是理论必然**：在金融序列中，Transformer 预测出的“平直线”并非优化失败，而是模型在逼近理论上的贝叶斯最优解（条件均值）。
*   **表达能力是双刃剑**：在低信噪比场景下，模型越复杂，越容易通过拟合噪声引入额外的预测方差，导致泛化性能严重退化。
*   **线性模型更稳健**：简单的线性模型由于参数极少，有效抑制了方差溢出，在金融预测中表现出更强的鲁棒性。

#### 7. 优点
*   **理论深度**：为“深度学习在金融预测中失效”这一普遍观察提供了严谨的数学解释，超越了简单的经验总结。
*   **视角独特**：将预测问题视为“轨迹到轨迹”的学习，并重点分析了条件期望退化对高容量模型的影响。
*   **实验直观**：通过误差分布的对比，清晰地展示了 Transformer 模型在几乎所有预测窗口下都存在性能劣势。

#### 8. 不足与局限
*   **模型覆盖有限**：虽然 PatchTST 是目前的 SOTA，但未讨论正则化技术（如 Dropout, Weight Decay）或早停法是否能有效缓解这种方差溢出。
*   **损失函数单一**：研究仅限于平方损失（MSE）。作者在结论中提到，如果改用生成式模型（如扩散模型）学习全概率分布而非条件均值，可能会有不同结果，但本文未对此进行实验验证。
*   **应用限制**：结论主要适用于“弱条件结构”序列，对于具有强季节性或趋势性的工业/气象数据，Transformer 的优势依然存在，读者需注意场景区分。

（完）
