---
title: "NLPOpt-Net: A Learning Method for Nonlinear Optimization with Feasibility Guarantees"
title_zh: NLPOpt-Net：一种具有可行性保证的非线性优化学习方法
authors: "Bimol Nath Roy, Rahul Golder, MM Faruque Hasan"
date: 2026-04-30
pdf: "https://arxiv.org/pdf/2605.00260v1"
tags: ["query:optai"]
score: 9.0
evidence: 具有约束满足保证的非线性优化学习方法
tldr: 本文提出NLPOpt-Net，一种具有可行性保证的非线性优化学习方法，通过无监督学习结构解决带约束的非线性规划问题。其核心是结合神经网络与多层投影结构，确保在逼近最优解的同时严格满足约束，并显著提升大规模问题的求解效率与精度。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.00260v1/fig-001.webp\", \"caption\": \"Figure 2: Graphical interpretation of the approximated layer. Left: the linearized model represents a polytope confining the original convex feasible domain. Middle: The neural network is learning to minimize the loss within the polytope, Right: the local exactness of the problem after linearization.\", \"page\": 25, \"index\": 1, \"width\": 829, \"height\": 277}, {\"url\": \"assets/figures/arxiv/2605.00260v1/fig-002.webp\", \"caption\": \"Table 7: Performance across problem classes and parameter settings. The first row provides the active set agreement for the results shown in Section 3. Other rows shows results on QP with 100 variables and the common structure with 50 equality, 50 inequality and 50 parameters with varying one of the property at a time. We cap the dual variables to 1× 10−6\", \"page\": 15, \"index\": 2, \"width\": 825, \"height\": 216}, {\"url\": \"assets/figures/arxiv/2605.00260v1/fig-003.webp\", \"caption\": \"Table 6: Results on QP task for 100 variables, 50 parameters, 50 inequality constraints and 50 parameters as the number of samples taken for training is varying. We consider total 2000 instances and for training we vary the training fraction as 0.8, 0.6, 0.4, 0.2.\", \"page\": 15, \"index\": 3, \"width\": 822, \"height\": 451}, {\"url\": \"assets/figures/arxiv/2605.00260v1/fig-004.webp\", \"caption\": \"Table 4: Results on QP task for 100 variables, 50 parameters and 50 inequality constraints as the number of equality constraints varies as 10, 30, 50, 70, 90.\", \"page\": 14, \"index\": 4, \"width\": 823, \"height\": 392}, {\"url\": \"assets/figures/arxiv/2605.00260v1/fig-005.webp\", \"caption\": \"Table 5: Results on QP task for 100 variables, 50 parameters and 50 equality constraints as the number of inequality constraints varies as 10, 30, 50, 70, 90.\", \"page\": 14, \"index\": 5, \"width\": 825, \"height\": 381}, {\"url\": \"assets/figures/arxiv/2605.00260v1/fig-006.webp\", \"caption\": \"Figure 1: Illustration of the NLPOpt-Net framework. The forward pass includes NN followed by k-layered projection. The backward pass uses custom VJP route. The NN is optimized with the original objective along with a regularized soft loss and a consistency loss.\", \"page\": 3, \"index\": 6, \"width\": 582, \"height\": 356}, {\"url\": \"assets/figures/arxiv/2605.00260v1/fig-007.webp\", \"caption\": \"Table 3: Results on QP task for 100 variables, 50 equality and 50 inequality constraints as the number of parameters varies as 10, 25, 50, 75, 100. We compare the performance of NLPOpt-Net with DC3 and the optimizer according to objective value, maximum/mean equality and inequality violation and inference time per batch. Standard deviation across 5 runs are shown in parenthesis.\", \"page\": 13, \"index\": 7, \"width\": 825, \"height\": 390}, {\"url\": \"assets/figures/arxiv/2605.00260v1/fig-008.webp\", \"caption\": \"Table 2: Results for parameterized optimization problems in terms of objective, constraint violation and time. The standard deviations across different runs are reported in the parenthesis. After training, both training and validation instances are tested. Values are rounded off to three decimals.\", \"page\": 8, \"index\": 8, \"width\": 823, \"height\": 546}]"
motivation: 目标是为带约束的非线性优化问题构建一种既能学习参数化解又能确保约束满足的高效方法。
method: 采用神经网络结合多层投影结构，通过改进的拉格朗日和一致性损失驱动优化，并使用修改的Chambolle-Pock算法保证可行性。
result: 该方法在求解大规模凸QP、QCQP和非凸优化问题上达到了机器精度级约束满足和近零最优性差距。
conclusion: NLPOpt-Net在保持约束可行性的同时，实现几乎零最优性差距，显著改善推理速度和精度。
---

## 摘要
非线性参数优化网络（NLPOpt-Net）是一种用于求解带约束非线性规划（NLP）的无监督学习架构。给定NLP的结构，它能够学习具有约束满足保证的参数化解映射。该架构由一个骨干神经网络（NN）和一个多层（$k$层）投影组成。神经网络通过包含改进的拉格朗日函数以及一致性损失的损失函数驱动优化，而投影通过将神经网络的预测映射到原始约束流形上以确保可行性。不同于典型的距离最小化方法，我们的投影利用原始NLP的局部二次近似。在某些条件（如凸性）下，该投影具有下降性质，进一步改进神经网络的预测。NLPOpt-Net在前向传播过程中采用一种无需求逆的改进Chambolle-Pock算法来求解带约束的二次投影问题，并利用隐函数定理实现高效的反向传播。固定的投影结构还使得在训练完成后可以将神经网络与投影解耦。NLPOpt-Net能够以接近零的最优性间隙和约束违反接近机器精度的水平求解大规模凸QP、QCQP、NLP以及非凸问题。此外，它还能近乎准确地预测活跃集及相应的对偶变量，从而为多参数规划提供可扩展的方法。将投影部分用C语言编译，相较于JAX显著提高了推理速度。我们提供了代码和具备GPU支持的NLPOpt-Net打包版本，供直接使用。

## Abstract
Nonlinear Parametric Optimization Network (NLPOpt-Net) is an unsupervised learning architecture to solve constrained nonlinear programs (NLP). Given the structure of an NLP, it learns the parametric solution maps with guaranteed constraint satisfaction. The architecture consists of a backbone neural network (NN) followed by a multilayer ($k$-layered) projection. While the NN drives toward optimality through a loss function consisting of a modified Lagrangian augmented with a consistency loss, the projection ensures feasibility by projecting the NN predictions in the original constraint manifold. Instead of typical distance minimization, our projection exploits local quadratic approximations of the original NLP. Under certain conditions (such as convexity), the projection has a descent property, which improves the NN predictions further. NLPOpt-Net deploys an inversion-free, modified Chambolle-Pock algorithm to solve the constrained quadratic projections during the forward pass and uses the implicit function theorem for efficient backpropagation. The fixed structure of the projection further allows decoupling of the NN and the projection once the training is complete. NLPOpt-Net solves large-scale convex QP, QCQP, NLP, and nonconvex problems with near zero optimality gap and constraint violations reduced to machine precision. Additionally, it provides near accurate prediction of the active sets and corresponding dual variables, thereby enabling a scalable approach for multiparametric programming. Compiling the projection in C provides order of magnitude improvement in inference time compared to JAX. We provide the codes and NLPOpt-Net as a ready to use package that includes GPU support.

---

## 论文详细总结（自动生成）

# NLPOpt-Net：一种具有可行性保证的非线性优化学习方法 — 论文总结

---

## 一、研究背景与核心问题

- **研究动机**  
  本文的目标是解决**带有复杂约束的非线性规划（NLP）**问题在深度学习场景中的求解难题。传统优化器（如 IPOPT、CONOPT、Knitro、SNOPT）虽然可以精确求解单个 NLP 实例，但难以在**多参数场景**中快速重复计算，限制了其在实时优化控制、模型预测控制等应用中的可扩展性。

- **核心挑战**  
  深度学习方法在这类问题上存在两个主要困难：
  1. **缺乏对约束可行性的显式保证**——传统神经网络在学习参数到解映射时通常采用软约束（Penalty-based）方式，容易出现物理不合理或违反约束的结果。
  2. **难以保持最优性和可行性的一致性**——惩罚项权重调节困难，导致性能不稳定。

- **论文目标**  
  提出一种能够：
  - 保证约束严格满足（Feasibility Guarantee）；
  - 同时逼近最优解（Near-optimality）；  
  的可扩展神经网络架构，用于多参数非线性优化。

---

## 二、方法论：NLPOpt-Net 的核心思想与技术流程

### 1. 架构设计
NLPOpt-Net 由两部分组成：
- **骨干神经网络（Backbone NN）**  
  用于预测初步解 $\hat{y}$；
- **多层投影模块（k-layer Projection）**  
  将神经网络预测投影到原始约束流形，确保输出解 $\tilde{y}$ 满足约束并保持下降性质。

### 2. 关键思想
- **改进的拉格朗日损失函数**  
  结合目标函数、约束项与一致性损失（Consistency Loss）：
  \[
  L = f(\tilde{y}) + ||\tilde{\lambda}^\top h(x, \tilde{y})||^2 + \tilde{\mu}^\top \text{ReLU}(g(x,\tilde{y})) + \alpha ||\hat{z} - \tilde{z}||^2
  \]
  其中 $\alpha$ 控制一致性项，以减小近似误差，保证训练过程稳定。

- **局部二次投影（Local Quadratic Projection）**  
  在投影层中，不采用传统欧式距离最小化，而是基于原始目标函数的局部二次近似：
  \[
  \tilde{y} = \arg\min_{y\in S} f(x,\hat{y}) + J^\top(y-\hat{y}) + \frac{1}{2}(y-\hat{y})^\top H_d (y-\hat{y})
  \]
  其中梯度 $J$ 与对角化 Hessian $H_d$ 保证该投影具有**下降性质**（Descent Property）。

- **求解算法：修改版 Chambolle-Pock (CP) 算法**  
  利用一阶原对偶迭代求解上述凸二次投影问题，实现：
  - **无需求逆（Inversion-free）**计算；
  - **GPU 可并行化**；
  - **迭代终止条件明确**（原始残差、对偶残差与对偶间隙的阈值）。

- **反向传播中的隐函数定理**  
  为避免直接通过迭代展开求导造成的计算瓶颈，采用**隐函数定理**构建高效的梯度传播路径（通过求解线性系统代替矩阵求逆）。

- **投影层解耦与编译优化**  
  完成训练后，投影层可使用 C 语言 Ahead-of-Time (AOT) 编译，显著加快单次推理速度，可达到与传统优化器（OSQP）同量级的响应时间。

### 3. 总体算法流程
包括训练阶段（Algorithm 3）与推理阶段（Algorithm 2）：
1. 神经网络前向预测；
2. 执行 k 层投影；
3. 使用 CP 算法求解每层 QP 子问题；
4. 利用隐函数定理反向传播；
5. 采用 Adam 优化器更新参数。  

---

## 三、实验设计

### 1. 实验场景与任务类型
在多类优化任务上验证：
- **凸问题**：Quadratic Program (QP)、Quadratically Constrained QP (QCQP)、Convex Nonlinear Program；
- **非凸问题**：包括带正弦项的复杂约束非凸函数。

### 2. 数据与参数设置
每个任务生成 2000 个实例，参数维度为 50、决策变量为 100，约束包括：
- 50 个等式（Equality）；
- 50 个不等式（Inequality）。

参数分布从均匀分布中采样，保证可行性（详见附录 A 的构造方法）。

### 3. 对比方法
与以下代表性模型进行了系统比较：
- **Optimizer**：CVXPY 环境 + OSQP/SCS/SLSQP；
- **NN**：普通软约束神经网络；
- **Eq-NN**：基于部分预测和等式补全；
- **DC3**：目前主流的约束补全与修正框架。

### 4. Benchmark 指标
- **Optimality Gap**：与最优解的差距；
- **Feasibility**：约束违反程度（最大及平均值）；
- **Inference Speed**：推理时间（单实例或批量）。

---

## 四、资源与算力

- **训练平台**  
  使用一台拥有：
  - Dual Intel Xeon 6248R (48 核, 3.0GHz)
  - 单块 NVIDIA A100 GPU (40GB)
  - 360 GB 内存
  的节点。

- **训练与测试配置**
  - 每种任务训练 2000 个 epoch；
  - 批量大小为 400；
  - 同一任务在 5 个不同随机种子下重复验证。

- 若干 CPU 实验使用 Intel i7-13700 (24 核) 处理器，验证投影层的推理速度。

---

## 五、实验数量与充分性

- **主要实验数量**
  - 4 类主任务（QP、QCQP、Convex NLP、Nonconvex NLP）；
  - 5 组附加实验（参数维度、约束数量、训练样本比例、活跃集预测等）；
  - 总计超过 **20+ 对比表格与指标结果**。

- **实验充分性**
  - 各类问题均进行训练与验证集测试；
  - 对比方法均为开源实现，参数设置一致；
  - 评估指标涵盖准确性、可行性与速度。  
  → 实验设计被认为**系统且公平**。

---

## 六、主要结论与发现

- NLPOpt-Net 可在凸问题上实现：
  - **近零最优性差距（≈0.08%）**；
  - **约束违反接近机器精度（≈0）**；
  - 推理时间与 OSQP 相同量级（2 ms）。
- 在非凸任务中亦表现出稳健性能，比 DC3 方法更稳定。
- 投影的下降性保证使得投影之后的解不会劣于初始预测。
- 能够有效预测约束活跃集与对应对偶变量（准确率超过 99%）。
- C 编译的投影模块在单实例推理上相比 JAX 实现提升了约 **50 倍速度**。

---

## 七、方法与实验亮点

- **方法层面**
  - 创新性地将“局部二次近似”用于投影而非欧式距离，保持下降特性；
  - 投影与神经网络解耦，提高部署效率；
  - 使用隐函数定理加速反向传播，避免迭代求导开销；
  - 整合一阶原对偶算法（CP），实现稳定的无需求逆解法；
  - 架构完全**无监督训练**，直接利用优化问题结构学习。

- **实验层面**
  - 提供可执行的开源包，与 GPU 支持；
  - 展现出与传统优化器竞争的解精度，同时保留深度学习的高速预测优势；
  - 全面比较不同约束与参数配置下的性能一致性。

---

## 八、不足与局限

- **适用范围**：理论证明主要依赖于凸性假设；非凸问题的性能尚无严格收敛保证。
- **计算开销**：多层投影导致推理时间随投影层数线性增长（如 k=5 时比优化器慢约 9 倍）。
- **投影参数设置（ρ、α）**：需人工调优，不同问题敏感度较高。
- **数据生成**：使用合成问题，缺乏真实工业场景验证；
- **可扩展性风险**：尽管理论可扩展至大规模问题，但在极高维、复杂非线性约束下的表现仍需进一步实验。

---

（完）
