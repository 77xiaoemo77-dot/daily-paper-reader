---
title: Towards interpretable AI with quantum annealing feature selection
title_zh: 面向可解释人工智能的量子退火特征选择研究
authors: "Francesco Aldo Venturelli, Emanuele Costa, Sikha O K, Bruno Juliá-Díaz, Miguel A. González Ballester, Alba Cervera-Lierta"
date: 2026-04-28
pdf: "https://arxiv.org/pdf/2604.25649v1"
tags: ["query:qmlco"]
score: 9.0
evidence: 利用量子退火解决深度学习中的组合特征选择问题
tldr: 本文提出一种基于量子退火的特征选择方法，用于解释卷积神经网络的图像分类决策。研究通过将特征图重要性选择转换为量子优化问题求解，提高了解释结果的可分性与透明度，并分析了量子退火的计算特性与有效性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.25649v1/fig-001.webp\", \"caption\": \"FIG. 4. Energy gap ∆min distribution for Nf = 16, for τ = 50 and ∆s = 0.01 across all 200 samples of the test set. In purple, the cumulative distribution tells us about how likely we find values of the minimum gap.\", \"page\": 6, \"index\": 1, \"width\": 443, \"height\": 305}, {\"url\": \"assets/figures/arxiv/2604.25649v1/fig-002.webp\", \"caption\": \"FIG. 3. Qualitative comparison example of GradCAM and GradCAM++ with our QA-fGradCAM protocol for Nf = 16 and τ = 50. The selected representations capture relevant regions and seem to identify parts of the images that are misinterpreted by the state-of-the-art methods.\", \"page\": 6, \"index\": 2, \"width\": 969, \"height\": 459}, {\"url\": \"assets/figures/arxiv/2604.25649v1/fig-003.webp\", \"caption\": \"FIG. 9. Average of ∆min for different Nf as a function of the filtered FMs d. The scaling of the gap follows ∆min ∝ d−1 which is estimated by a linear regression. The error bars correspond to the first and third percentile of the entire data values on the specific dimension d. The distinct crosses represent the smallest gap ∆∗, i.e. the hardest instances to simulate.\", \"page\": 11, \"index\": 3, \"width\": 504, \"height\": 272}, {\"url\": \"assets/figures/arxiv/2604.25649v1/fig-004.webp\", \"caption\": \"FIG. 8. Gaussian fit of the Jpq distribution across all dataset. The distribution shows a Gaussian shape. The peaked bin corresponding to 0 value of Jpq represents the diagonal term of the cosine similarity matrix.\", \"page\": 10, \"index\": 4, \"width\": 486, \"height\": 314}, {\"url\": \"assets/figures/arxiv/2604.25649v1/fig-005.webp\", \"caption\": \"FIG. 7. (Top) average Drop % for SA with different β. (Down) bit-string length as a function of β, divided by Nf = 512. As visible, by increasing the value of β the linear term becomes more important within the QUBO formulation, and we converge to the results we would obtain with standard GradCAM and GradCAM++.\", \"page\": 10, \"index\": 5, \"width\": 372, \"height\": 501}, {\"url\": \"assets/figures/arxiv/2604.25649v1/fig-006.webp\", \"caption\": \"FIG. 1. Representation of the FS algorithm reformulated as a QUBO problem. 1−2) A single image is fed into a CNN that has been previously trained. From the last convolutional layer we extract the FMs f (a) ij (x), with (a) ∈ Nf . 3) A one-hot encoding is applied to the FMs that contribute positively to the gradient. 4) We construct the global problem Hamiltonian, prepare the initial quantum state in superposition of all possible Nf combinations and we use QA to evolve the state towards the minimum of the QUBO Hamiltonian. 5) Finally, we sample the resulting bitstring, which corresponds to the explanation map containing the FMs that satisfy the constraints imposed by the problem Hamiltonian: maximizing the positive contribution of each FM while minimizing their geometrical similarity. This pipeline is repeated for M images from the test set.\", \"page\": 3, \"index\": 6, \"width\": 1021, \"height\": 554}, {\"url\": \"assets/figures/arxiv/2604.25649v1/fig-007.webp\", \"caption\": \"FIG. 12. Comparison between state-of-the-art methods and the FS-based on QA for some samples of each image class.\", \"page\": 13, \"index\": 7, \"width\": 814, \"height\": 1157}, {\"url\": \"assets/figures/arxiv/2604.25649v1/fig-008.webp\", \"caption\": \"FIG. 11. Overlap between subsets of single FMs sampled by the QA for airplane, ship and truck classes. The algorithm is repeated for the same image sample with τ = 10 to verify how much variability in sampling the same bit string the model leads to. From left to right the selected FMs are [0, 1, 15] for the airplane class that we consider as pivots.\", \"page\": 12, \"index\": 8, \"width\": 1040, \"height\": 630}, {\"url\": \"assets/figures/arxiv/2604.25649v1/fig-009.webp\", \"caption\": \"TABLE I. Comparison between GradCAM, GradCAM++ and our QA-fGradCAM protocol in terms of the Average Drop % per image class. The QA simulation takes τ = 50 and nshots = d2, where d is the number of filtered FMs for each image. Out method performs better than GradCAM and in most classes also better than GradCAM++.\", \"page\": 5, \"index\": 9, \"width\": 948, \"height\": 381}, {\"url\": \"assets/figures/arxiv/2604.25649v1/fig-010.webp\", \"caption\": \"FIG. 2. (Top) Class–class correlation map for the FS algorithm for a ResNet-18 constrained to Nf = 16 FMs in the final layer and trained for 20 epochs. Warmer colors indicate stronger overlap between FMs. The matrix is obtained by computing the scalar product between the distributions of the activated FMs per image class, i.e. the Bhattacharya coefficient. In the QA simulation, we set τ = 50 and nshots = d2, where d is the number of FMs that contribute positively to the output’s gradient. A full diagonal matrix indicates complete orthogonality among classes. Values below 0.10 are rounded to 0. (Bottom) Class–class correlation map for FS algorithm using SA for the full ResNet-18 model trained for 100 epochs. For Nf = 512, the SA is able to find a good disentanglement between hidden learned representations.\", \"page\": 5, \"index\": 10, \"width\": 1051, \"height\": 501}]"
motivation: 深度学习在关键任务中的可靠性依赖于可解释性，因此需要理解模型预测的依据以提升信任度和公平性。
method: 该研究通过将特征图选择问题编码为量子约束优化问题，并利用量子退火求解，以解释卷积神经网络在图像分类中的决策。
result: 实验表明，该方法相比GradCAM和GradCAM++能显著提高类别区分度和模型推理透明度。
conclusion: 结果显示该方法可提升模型解释性能，并提供了量子算法在AI可解释性中的理论与实践价值。
---

## 摘要
深度学习模型被广泛应用于关键领域，其中错误可能导致严重后果。因此，理解模型如何以及为何做出预测至关重要。这种理解可以帮助检验模型是否学习到了正确的模式、发现数据中的偏差、改进模型设计，并建立可信的系统。本文提出了一种用于图像分类任务中解释卷积神经网络的新方法。该方法通过选择对每次预测最重要的特征图来实现解释。为了解决这一组合优化问题，我们将其编码为一个量子受约束优化问题，并提出通过量子退火进行求解。我们将本方法与最先进的可解释人工智能技术（尤其是 GradCAM 和 GradCAM++）进行了比较，观察到更好的类别解缠性能，即模型的决策边界更加清晰，推理过程更加透明。这表明我们的方法提高了解释质量，使理解模型在特定预测中依赖的特征更加容易。此外，我们研究了量子退火算法的计算行为，具体分析了系统在计算过程中最小能隙以及算法找到正确解的概率。这些分析为该方法在实践中有效运行提供了理论上的见解。

## Abstract
Deep learning models are used in critical applications, in which mistakes can have serious consequences. Therefore, it is crucial to understand how and why models generate predictions. This understanding provides useful information to check whether the model is learning the right patterns, detect biases in the data, improve model design, and build systems that can be trusted. This work proposes a new method for interpreting Convolutional Neural Networks in image classification tasks. The approach works by selecting the most important feature maps that contribute to each prediction. To solve this combinatorial problem, we encode it into a quantum constrained optimization problem and propose to solve it using quantum annealing. We evaluate our method against the state-of-the-art explainable AI techniques, specifically GradCAM and GradCAM++, and observe an improved class disentanglement, i.e. the model's decision boundaries become more distinct and its reasoning more transparent. This demonstrates that our approach enhances the quality of explanations, making it easier to understand which features the model relies on for specific predictions. In addition, we study the computational behavior of the quantum annealing algorithm. Specifically, we analyze the minimum energy gap of the system during computation and the probability that the algorithm finds the correct solution. These analyses provide theoretical insight into why the method works effectively in practice.

---

## 论文详细总结（自动生成）

# 面向可解释人工智能的量子退火特征选择研究 — 深度总结

---

## 一、核心问题与研究动机

- **研究背景**：  
  深度学习（DL）在计算机视觉等领域取得显著成功，但其“黑箱”性质使得模型的预测过程难以解释。在安全关键领域（如医学诊断、自动驾驶）中，缺乏解释性会导致信任与风险问题。

- **核心问题**：  
  现有解释型方法（如 GradCAM、GradCAM++）对卷积神经网络（CNN）的可视化解释仅给出聚合的显著性区域，而无法区分具体特征图（Feature Maps, FMs）的作用。  
  作者提出从模型内部学习的特征图出发，选择对预测最重要的子集，实现 **特征级解释**。

- **研究目标**：  
  通过量子计算中的**量子退火（Quantum Annealing, QA）**求解特征选择这一组合优化问题，从而获得更透明、更具区分度的模型解释。

---

## 二、方法论：核心思想与技术细节

### 1. 核心思想

- 将 CNN 的解释问题转化为一个 **特征图选择（Feature Selection, FS）** 任务。
- 使用量子优化框架，将 FS 编码为 **二次无约束二进制优化（QUBO）** 问题。
- 利用量子退火算法求解 QUBO，从而选出对某一预测最有贡献、且相互独立的特征图集合。

### 2. 关键技术步骤

1. 从已训练的 CNN（如 ResNet-18）最后的卷积层提取特征图 \( f^{(a)}_{ij}(x) \)。  
2. 计算输出对每个特征图的梯度平均值：
   \[
   \alpha_a = \frac{1}{H_f W_f} \sum_{ij} \frac{\partial z}{\partial f^{(a)}_{ij}}
   \]
   以衡量特征图的重要度（类似 GradCAM 权重）。
3. 筛选正梯度的特征图：  
   \(\{\tilde{f}\} = \{f^{(a)} | \alpha_a > 0\}\)
4. 计算筛选特征图之间的**余弦相似度** \( J_{pq} \)，以衡量冗余。
5. 构建 QUBO 哈密顿量：
   \[
   \hat{H}_{QUBO} = (1 - β)\frac{1}{2}\sum_{pq} J_{pq}\hat{n}_p \hat{n}_q + β\sum_p h_p\hat{n}_p
   \]
   其中：
   - \(h_p = \tilde{\alpha}_p / \max(\tilde{\alpha}_p)\)
   - \(β\) 控制重要度和冗余之间的平衡。
6. 使用量子退火演化规则：
   \[
   \hat{H}(s) = A(s)\hat{H}_D + B(s)\hat{H}_{QUBO}, \quad A(s)=1-s, B(s)=s
   \]
   逐步演化到最终的最优特征组合（即解释图）。

7. 输出最终选中的特征图子集对应的解释掩膜。

---

## 三、实验设计

- **数据集**：  
  使用 **STL-10** 数据集（10 类，96×96 RGB 图像，经上采样至 224×224），共 5000 张图像。
  
- **模型结构**：  
  基于 **ResNet-18**，但修改最后的卷积块，使输出特征图数量可控（分别实验 Nf = 16、24、32、512）。

- **对比方法**：  
  - GradCAM  
  - GradCAM++  
  - 以及作者提出的 **QA-filtered GradCAM（QA-f GradCAM）**

- **量化指标**：  
  - 类间相关矩阵（Bhattacharyya 系数）  
  - Average Drop %（平均置信度下降率，用于衡量解释可靠性）

- **实验场景**：  
  既使用量子退火模拟（QA），也对比经典算法下的**模拟退火（SA）**版本。

---

## 四、资源与算力

- 论文未明确说明使用的 GPU 或量子硬件配置。  
- 作者指出目前实验在 **经典计算机上模拟量子退火**，故受限于特征图数量（最多 Nf=32 时仍可仿真）。  
- 若有真实量子退火机（如 D-Wave 系统）可支持上千量子比特，则能扩展至完整 Nf=512 情况。

> ✅ 未给出训练 GPU 型号和算力信息，仅提到训练 20 个 epoch，验证集准确率为 94%。

---

## 五、实验数量与充分性

- 进行了多组实验：
  - 不同特征图数量（16、24、32、512）以验证方法的可扩展性。
  - 对比 QA 与 SA 的物理特性（能隙分布、保真度、退火时间等）。
  - 类间的特征图相关性分析（10 类图像的相关矩阵）。
  - 各类的 Average Drop % 定量对比（见表 I）。
  - 附录中还分析了复杂度随特征数量的标度关系。

- **实验充分性**：  
  对量子退火的物理层、解释结果质量及算法性能均进行了量化分析，但仅实验于单一数据集（STL-10），外推仍需进一步验证。

---

## 六、主要结论与发现

- **性能提升**：  
  QA-f GradCAM 在 Average Drop % 上平均优于 GradCAM 和 GradCAM++，说明解释更精确、影响更小。
- **分离度增强**：  
  类间相关矩阵更接近对角形式，模型决策边界更清晰。
- **理论分析**：  
  量子退火中的最小能隙分布集中在较高值（10⁻¹），意味着算法稳定能达最优状态。
- **复杂性结论**：  
  QUBO 转换后呈现 Sherrington-Kirkpatrick 模型的高斯耦合分布，具备可处理的均场性质。

---

## 七、方法与实验的亮点

- ✅ **创新性强**：将量子物理的退火过程用于 AI 解释性问题，为模型提供更高层次理解力。  
- ✅ **解释粒度更细**：从“像素级”扩展到“特征图级”，可精确辨别哪些内部表示起作用。  
- ✅ **理论与实验结合**：包含能隙、保真度、退火演化过程的物理分析，提供算法有效性解释。  
- ✅ **结果透明性提升**：实验图像显示比传统方法更能对应真实物体的关键区域。  

---

## 八、不足与局限

- ⚠ **量子模拟限制**：目前基于经典仿真，无法验证真实量子硬件的加速或扩展性。  
- ⚠ **样本与数据集单一**：仅验证于 STL-10，缺乏多样任务或跨模型测试。  
- ⚠ **参数敏感性**：QUBO 的 β 参数需精调，不同 β 下结果差异显著。  
- ⚠ **无消融实验**：未展示去除部分项（如余弦相似度项）后的效果比较。  
- ⚠ **算力成本未知**：论文未报告训练时长、硬件配置或能耗情况。  

---

**（完）**
