---
title: "RAG-Enhanced Kernel-Based Heuristic Synthesis (RKHS): A Structured Methodology Using Large Language Models for Hardware Design"
title_zh: RAG增强的基于核的启发式合成（RKHS）：一种利用大型语言模型进行硬件设计的结构化方法论
authors: "Shiva Ahir, Alex Doboli"
date: 2026-04-28
pdf: "https://arxiv.org/pdf/2604.26153v1"
tags: ["query:optai"]
score: 9.0
evidence: 大语言模型驱动的硬件设计可重用优化启发式算法合成
tldr: 本文提出RAG增强的核驱动启发式综合方法（RKHS），通过结合检索增强生成（RAG）、紧凑启发模板及自反馈迭代的LLM优化机制，系统化地生成可复用硬件设计启发策略，在高层综合调度中显著提升性能。
source: arxiv
selection_source: fresh_fetch
motivation: 当前EDA工具的启发式设计依赖专家经验，亟需利用LLM系统化自动生成优化策略。
method: 结合RAG、启发模板和LLM自反馈迭代形成结构化启发式综合流程。
result: "在高层综合的延迟最小化任务中，原型系统相比基线调度器平均缩短调度长度11%，运行时间仅增加1.3倍，并能推广到其它EDA优化问题。"
conclusion: RKHS结构化的检索与生成循环可以改善调度性能并具备推广性。
---

## 摘要
启发式设计支撑着现代电子设计自动化（EDA）工具，但要制定有效的布局、布线和调度策略，需要大量的专业知识。我们研究大型语言模型（LLM）如何系统性地合成可复用的优化启发式算法，而不仅仅是一次性代码生成。我们提出了RAG增强的基于核的启发式合成（RKHS）方法，它结合了检索增强生成（RAG）、紧凑的核启发式模板以及受迭代自反馈启发的LLM驱动精炼循环。应用于高层次综合（HLS）中的最小延迟列表调度任务时，原型系统相比基准调度器可将平均调度长度减少最多11%，运行时间开销仅为1.3倍。此外，该结构化的检索–合成循环可推广至其他EDA优化问题。

## Abstract
Heuristic design upholds modern electronic design automation (EDA) tools, yet crafting effective placement, routing, and scheduling strategies entails substantial expertise. We study how large language models (LLMs) can systematically synthesize reusable optimization heuristics beyond one-shot code generation. We propose RAG-Enhanced Kernel-Based Heuristic Synthesis (RKHS), which integrates retrieval-augmented generation (RAG), compact kernel heuristic templates, and an LLM-driven refinement loop inspired by iterative self-feedback. Applied to latency-minimizing list scheduling in high-level synthesis (HLS), a prototype reduces average schedule length by up to 11 percent over a baseline scheduler with only 1.3x runtime overhead, and the structured retrieval-synthesis loop generalizes to other EDA optimization problems.

---

## 论文详细总结（自动生成）

# RAG增强的基于核的启发式合成（RKHS）论文详细总结

---

## 一、核心问题与整体含义

- **研究背景**  
  当代电子设计自动化（EDA）工具依赖于专家手工设计启发式算法，用于布局、布线、调度和综合等NP难问题。这些启发式往往局限于特定场景，难以复用与扩展。
  
- **研究动机**  
  随着电路规模和复杂度增加，人工启发式设计愈发不具可扩展性。论文探索利用**大型语言模型（LLM）**在硬件设计中自动生成优化方案，系统化合成“可执行、可解释、可迁移”的启发式策略，突破仅依赖一次性代码生成的旧范式。

---

## 二、方法论

### 2.1 核心思想
提出一种**RAG增强的基于核的启发式合成（RKHS）框架**，将结构检索与LLM驱动的合成相结合，实现“结构化启发式自动生成”。

### 2.2 方法构成
RKHS由三大核心组件构成：

1. **检索增强生成（Retrieval-Augmented Generation, RAG）**
   - 从先验设计数据中检索结构上相似的“核（kernel）”模式，即代表可复用的调度启发模板。  
   - 使用**余弦相似度**定义检索度量：  
     \[
     sim(G, K) = \frac{\langle f(G), f(K) \rangle}{\|f(G)\|\|f(K)\|}
     \]
     其中 \( f(G) \) 为图嵌入，\( f(K) \) 为核特征。

2. **核模板与结构特征**
   - 每个核包含两个部分：
     - **结构特征签名**：如关键路径、扇出、重汇合等统计属性；
     - **启发式模板**：表示在该结构下的优先调度规则（例如 `π(v) = α·crit(v) + β·fanout(v) − γ·level(v)`）。
   - 这些模板来源于对训练任务图（DAG）的结构性子图挖掘与聚类。

3. **LLM驱动的启发式合成与自反馈循环**
   - 将检索到的核信息、任务约束、示例等封装为结构化 prompt 供 LLM（如GPT-4）生成调度优先级函数代码。
   - 通过多次迭代（N=3），引入自反馈机制，根据失败案例与性能差异修正 prompt，逐步优化启发式表现。

### 2.3 算法流程（文字说明）
**算法 1：RAG-Enhanced Kernel-Based Heuristic Synthesis**

1. 初始化历史记录集 H；
2. 在每次迭代中从训练图集中采样批次；
3. 为每个图检索最相似的核集合；
4. 聚合这些核并形成结构化生成 prompt；
5. 调用 LLM 生成可执行启发式代码；
6. 在验证集上评估延迟与运行时间；
7. 生成反馈信息并在下次迭代中使用；
8. 最终选择综合性能最优的启发式 \(H^\star\)。

---

## 三、实验设计

- **实验场景**：在高层综合（High-Level Synthesis, HLS）中的延迟最小列表调度任务上验证。
- **数据集**：
  - 训练图集：\(|G_{train}| = 200\)
  - 验证图集：\(|G_{val}| = 50\)
  - 核库：\(|K| = 50\)
- **对比方法（Benchmark）**：
  - **基线调度器**：基于层级优先（level-based list scheduler）。
  - **Ablation 对照**：无检索、无结构模板、随机核选取。
  - 比较项包括平均延迟 (Latency)、运行时间 (Runtime) 与综合评分 (J值)。

---

## 四、资源与算力

- 文中未详细说明所使用的硬件资源，如 GPU 型号、训练时间或算力规模。
- 提到实现基于 **GPT-4**，但未指明调用平台或 API 方式，因此算力信息**未明确披露**。

---

## 五、实验数量与充分性

- **核心实验**：
  1. 启发式合成循环的性能迭代表（3次迭代）；
  2. **消融实验**（4种设置：Full RKHS、No Retrieval、No Motif、Random Kernel）。
- **实验充分性**：
  - 包含不同结构场景（4个典型验证图）；
  - 对照组设计较为完整；
  - 然而样本总体有限（50个验证图），规模偏小；
  - 尚未跨多个EDA任务（如布局、布线）进行验证，**广泛性有待提升**。

---

## 六、主要结论与发现

- RKHS 能系统性地从历史设计中抽取结构启发模板并在LLM中生成解释性调度规则。
- 在高层综合调度中：
  - 平均延迟减少 **约11%**；
  - 运行时间增长仅为 **1.3倍**；
  - 移除检索或模板机制显著降低性能与稳定性；
- 最优启发式可简化为线性组合形式：
  \[
  \pi(v) = α·crit(v) + β·fanout(v) − γ·level(v)
  \]
 其效果验证了互补结构信号（关键路径 + 扇出 + 层级深度）协同优化的价值。

---

## 七、优点

- **结构化框架**：将启发式设计形式化为检索–生成–反馈的闭环，具备可追溯性与确定性。
- **高可解释性**：生成的启发式具有明确数学结构，便于专家理解、审计与迁移。
- **方法可泛化**：可扩展至EDA其他问题（如版图优化、资源分配）。
- **自反馈强化**：通过迭代自我精炼提升可靠性，避免一次性生成的随机性。

---

## 八、不足与局限

- **算力与成本不明**：LLM生成环节依赖GPT-4，但具体资源未说明。
- **验证范围有限**：实验仅涵盖特定调度任务，未测试在真实工业级EDA系统上的适用性。
- **数据规模偏小**：训练与验证图数量有限，不足以评估跨技术节点的泛化。
- **检索机制可能依赖库质量**：核库规模与多样性会显著影响结果稳定性。
- **潜在偏差**：LLM生成可能受训练数据分布及提示设计偏差影响，可解释但不保证全局最优。

---

（完）
