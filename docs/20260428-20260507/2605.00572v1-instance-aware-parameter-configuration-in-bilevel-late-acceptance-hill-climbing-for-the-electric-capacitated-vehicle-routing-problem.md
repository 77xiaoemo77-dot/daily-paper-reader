---
title: Instance-Aware Parameter Configuration in Bilevel Late Acceptance Hill Climbing for the Electric Capacitated Vehicle Routing Problem
title_zh: 用于电动容量车辆路径问题的双层迟接受爬山算法中的实例感知参数配置
authors: "Yinghao Qin, Xinwei Wang, Mosab Bazargani, Jun Chen"
date: 2026-05-01
pdf: "https://arxiv.org/pdf/2605.00572v1"
tags: ["query:optai"]
score: 9.0
evidence: 元启发式爬山算法的实例感知参数配置
tldr: "本文针对电动容量约束车辆路径问题，提出基于实例特征的双层延迟接受爬山算法参数配置方法，通过离线调参生成实例特定标签并用回归模型预测未见实例的参数，实验结果表明相比全局调参可降低约0.28%的运输成本。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.00572v1/fig-001.webp\", \"caption\": \"Fig. 1. Illustrative solution to the E-CVRP.\", \"page\": 2, \"index\": 1, \"width\": 396, \"height\": 316}, {\"url\": \"assets/figures/arxiv/2605.00572v1/fig-002.webp\", \"caption\": \"TABLE VII PERFORMANCE OF B-LAHC WITH PREDICTED PARAMETERS ON THE TEST SET (GAPS ARE RELATIVE TO THE GLOBAL CONFIGURATION).\", \"page\": 7, \"index\": 2, \"width\": 822, \"height\": 599}, {\"url\": \"assets/figures/arxiv/2605.00572v1/fig-003.webp\", \"caption\": \"Fig. 2. Overview of the proposed instance-aware parameter prediction framework for b-LAHC.\", \"page\": 4, \"index\": 3, \"width\": 860, \"height\": 412}, {\"url\": \"assets/figures/arxiv/2605.00572v1/fig-004.webp\", \"caption\": \"TABLE II SUMMARY OF INSTANCE FEATURES USED FOR INSTANCE-AWARE PARAMETER PREDICTION\", \"page\": 4, \"index\": 4, \"width\": 1093, \"height\": 740}]"
motivation: 现有组合优化算法的性能高度依赖参数设置，单一全局调参难以适应不同实例的差异性。
method: 采用离线实例特定参数标注及基于特征的回归映射预测参数配置，以实现未见实例的参数自适应。
result: "实验显示，该方法在IEEE WCCI 2020基准及其扩展上平均降低0.28%的目标值，相比全局调参显著改进性能。"
conclusion: 实例感知的参数配置方法能有效提升算法在多样性实例上的优化表现并降低运输成本。
---

## 摘要
组合优化中的算法性能对参数设置极为敏感，而单一的全局调优配置往往难以充分利用实例的异质性。这一局限在电动容量车辆路径问题中尤为明显，该问题的实例在结构、需求模式及能量约束等方面差异显著。本文研究了电动容量车辆路径问题中双层迟接受爬山（Bilevel Late Acceptance Hill Climbing）这一先进元启发式算法的实例感知参数配置。我们采用离线调优过程获得与实例对应的参数标签，然后通过回归模型将这些标签从实例特征映射出来，以在算法执行前对未知实例进行参数预测。基于 IEEE WCCI 2020 基准及其扩展的实验结果表明，与全局调优配置相比，所提出的方法在八个保留测试实例上的平均目标值降低了 0.28%，这对应于数百万美元级运输运营中的显著成本降低。

## Abstract
Algorithm performance in combinatorial optimization is highly sensitive to parameter settings, while a single globally tuned configuration often fails to exploit the heterogeneity of instances. This limitation is particularly evident in the Electric Capacitated Vehicle Routing Problem, where instances differ in structure, demand patterns, and energy constraints. This paper investigates instance-aware parameter configuration for Bilevel Late Acceptance Hill Climbing, a state-of-the-art metaheuristic for the Electric Capacitated Vehicle Routing Problem. An offline tuning procedure is used to obtain instance-specific parameter labels, which are then mapped from instance features via a regression model to enable parameter prediction for unseen instances prior to execution. Experimental results on the IEEE WCCI 2020 benchmark and its extensions show that the proposed approach achieves an average objective value reduction of $0.28\%$ across eight held-out test instances relative to a globally tuned configuration. This corresponds to a significant cost reduction in multimillion-dollar transportation operations.

---

## 论文详细总结（自动生成）

# 用于电动容量车辆路径问题的双层迟接受爬山算法的实例感知参数配置 — 论文总结

---

## 一、研究核心问题与背景

- **核心问题**：组合优化算法的性能高度依赖参数设置，但传统上采用“单一全局调参”的方式忽略了不同实例间的结构异质性。
- **应用场景**：研究对象是 **电动容量车辆路径问题（E-CVRP）**，即在电动汽车的负载与能量约束下，规划最低成本的服务路线。
- **背景动机**：  
  - 不同 E-CVRP 实例在客户分布、充电站配置、能耗比例上差异巨大。
  - 单一参数配置难以在所有实例上同时表现优异。  
  - 因此，本研究希望发展**实例感知的参数配置（Instance-Aware Parameter Configuration, IAPC）**，在算法运行前根据实例特征预测最佳参数，用于提升搜索性能。

---

## 二、方法论与技术框架

### 1. 总体思路

提出一种 **基于实例特征的双层迟接受爬山算法（Bilevel Late Acceptance Hill Climbing, b-LAHC）** 参数预测框架：
- 通过**离线调参**获得各实例的最优参数标签；
- 再利用**回归模型**把实例特征映射到算法参数，实现对未见实例的参数预测。

### 2. 算法流程（文字说明）

分为两个阶段：

1. **离线训练阶段**：
   - 对多组训练实例进行独立参数调优（使用 IRACE 自动化调参工具）。
   - 获得每个实例的近似最优参数值（如历史长度 Lh、最大尝试次数 ηmax）。
   - 提取各实例的特征（如节点数、需求分布、图结构指标）。
   - 用这些“特征—最优参数”对训练回归模型。

2. **在线推理阶段**：
   - 对新实例提取相同特征；
   - 使用训练好的回归模型预测对应参数；
   - 将预测参数直接用于 b-LAHC 算法执行，无需再在线调参。

### 3. 关键技术细节

- **离线调参**：
  - 分两阶段进行：粗粒度搜索（Lh∈[300,20000]，ηmax∈[1,500]）与细粒度搜索；
  - 每阶段评估数千种配置，选取表现最优的中位数作为实例参数标签。
- **特征设计**（共 40 个左右）：
  - **基本信息**：客户数、充电站数、容量、能耗率等；
  - **图结构特征**：节点度、聚类系数、最小生成树权重、Laplacian 特征值、路径长度等；
  - **需求特征**：平均需求、需求方差、需求偏度、需求熵和高需求比例等。
- **预测模型**：使用 **Ridge Regression（岭回归）**，分别训练两个独立模型用于 Lh 和 ηmax；特征标准化处理后以 5 折交叉验证评估预测效果。
- **公式示例**：预测模型形式为  
  $$\hat{L}_h = \beta_0 + \sum_{j=1}^{d} \beta_j \tilde{x}_j$$  
  其中 $\tilde{x}_j$ 为标准化后的实例特征。

---

## 三、实验设计

- **数据来源**：
  - 总计 **41 个 E-CVRP 基准实例**：
    - IEEE WCCI 2020 EVRP Benchmark（17 个实例）
    - 扩展 Benchmark 集（24 个实例）
  - 来源包括四类经典 VRP 数据集：E、M、F、X 套件。
- **训练 / 测试划分**：
  - **33 个实例**作为训练集（用于模型学习与5折交叉验证）；
  - **8 个实例**作为独立测试集（用于泛化评估）。
- **基准与对比方法**：
  - 对比对象包括：
    - **单一全局调参（Global configuration）**；
    - **实例特定调参结果（Oracle tuning）**；
    - **本文预测的参数配置（Predicted configuration）**。
- **评价指标**：
  - 平均目标值（运输成本）、标准差；
  - 统计检验：采用 **Wilcoxon signed-rank test**, 显著性水平 α=0.05。

---

## 四、资源与算力

- **计算平台**：Sulis HPC 平台（AMD EPYC 7742, 64核, 2.25GHz）。
- **运行设定**：
  - 单线程执行；
  - 每次运行最大内存限制 1GB；
  - 每实例进行 10 次独立重复实验。
- **GPU/训练时长**：论文未提及 GPU 资源或模型训练时长，仅说明调参与回归模型训练在常规计算节点上完成。
  > 因此可推测该研究计算成本有限、可在通用CPU环境进行。

---

## 五、实验数量与充分性

- **离线调参实验**：
  - 每个实例至少经历 3,500 (粗调) + 8,000 (细调) 次参数评估；
  - 共针对 41 个实例开展两阶段调优，形成丰富训练数据。
- **模型验证实验**：
  - 共进行 **5 折交叉验证 + 独立测试评估**；
  - 每测试实例运行 10 次，结果取均值与方差。
- **结果的公平性**：
  - 所有方法在相同的硬件、编译器、评估预算（最大评估次数相同）条件下运行；
  - 使用非参数统计检验验证显著性。
  > 综合来看实验设计较为充分、控制较严，具有良好客观性。

---

## 六、主要结论与发现

1. **实例特定调参（oracle）** 比全局调参平均提升 0.27%，证明参数应随实例而异。
2. **预测参数（IAPC模型）** 比全局调参平均改进 0.28%，在 8 个测试实例中 **7 个提升、1 个轻微下降（+0.02%）**。
3. 改进具有统计显著性（p=0.0156），说明实例感知配置能真实提升优化性能。
4. 模型的特征贡献分析显示：  
   - **需求熵** 与 **高需求比例** 正相关于长历史长度参数 Lh，即需求更复杂时算法需更强探索。
   - 结构特征（例如图的聚类系数、边权分布）同样影响参数选择；  
   - 实例规模类特征影响较弱。
5. 总体上，模型预测的参数配置接近手工调优的“oracle”效果，为自动化、可泛化调参提供可行路径。

---

## 七、优点与创新亮点

- ✅ **实例感知框架创新性高**：首次在电动车辆路径问题中实现以实例为条件的参数预测。
- ✅ **方法组合合理**：结合 IRACE 自动调参 + 特征工程 + 岭回归，构成轻量却有效的框架。
- ✅ **特征设计丰富**：涵盖图结构与需求分布两类信息，解释力强。
- ✅ **模型可解释性好**：通过系数分析揭示特征对算法行为的影响，兼具理论与实践价值。
- ✅ **结果稳定且显著**：跨 8 个异质实例、提升显著、统计验证充分。
- ✅ **应用价值高**：改进幅度虽小（0.28%），但在实际物流成本中对应百万美元级效益。

---

## 八、不足与局限性

- ⚠ **使用线性回归模型（Ridge）可能欠拟合**：复杂实例间关系可能为非线性，若使用深度模型或集成学习可能进一步提高预测精度。
- ⚠ **训练样本数量有限（仅 33 个实例）**，模型泛化能力可能依赖实例分布。
- ⚠ **实验集中于一个特定问题（E-CVRP）**，尚未验证方法在其他组合优化问题上的通用性。
- ⚠ **模型未涉及动态调整**：预测参数为静态配置，无法应对运行时环境变化。
- ⚠ **算力限制**：仅在 CPU 上测试，未评估大规模分布式或更复杂模型的性能。
- ⚠ **改进幅度较小**：虽具显著性，但从算法研究角度仍需验证长期收益与鲁棒性。

---

**（完）**
