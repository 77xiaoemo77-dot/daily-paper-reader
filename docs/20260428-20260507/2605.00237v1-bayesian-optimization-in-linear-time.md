---
title: Bayesian Optimization in Linear Time
title_zh: 线性时间的贝叶斯优化
authors: "Jesse Schneider, William J. Welch"
date: 2026-04-30
pdf: "https://arxiv.org/pdf/2605.00237v1"
tags: ["query:optai"]
score: 7.5
evidence: 针对昂贵目标函数的使用高斯过程模型的贝叶斯优化
tldr: 本文提出了一种线性时间的贝叶斯优化方法，通过使用递归二叉划分来局部建模目标函数并调整获取策略，从而降低传统贝叶斯优化的立方复杂度并提升优化性能，实验显示在多个高维函数上均优于现有方法。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.00237v1/fig-001.webp\", \"caption\": \"Figure 24: Side-by-side boxplots of the results for the Levy test function in 10 dimensions. The vertical axis is on a logarithmic scale. The boxplots show the smallest objective-function value found, for both DiceOptim and TreeBO, at several points over the optimization process. Each boxplot represents the distribution over all 100 paired runs. At 0 subsequent observations the distributions are identical for both methods, because both methods are given identical sets of 100 initial points for each paired run. The boxplots show that TreeBO outperforms DiceOptim from beginning to end, and the gap between the two methods grows over time. The final pair of boxplots at 350 subsequent observations shows that the 75th percentile for TreeBO is less than the 25th percentile for DiceOptim. Moreover, the value of the objective function found on the worst run for TreeBO (the single red dot) is approximately equal to the 75th percentile for DiceOptim.\", \"page\": 25, \"index\": 1, \"width\": 1022, \"height\": 489}, {\"url\": \"assets/figures/arxiv/2605.00237v1/fig-002.webp\", \"caption\": \"Figure 25: Side-by-side boxplots of the results for the Michalewicz test function in 10 dimensions. The boxplots show the smallest objective-function value found, for both DiceOptim and TreeBO, at several points over the optimization process. Each boxplot represents the distribution over all 100 paired runs. At 0 subsequent observations the distributions are identical for both methods, because both methods are given identical sets of 100 initial points for each paired run. The boxplots show that DiceOptim outperforms TreeBO until roughly 300 subsequent observations. From that point on, TreeBO outperforms DiceOptim. The final pair of boxplots at 350 subsequent observations shows a slight advantage for TreeBO over DiceOptim in both the 25th–75th percentiles and in the outliers.\", \"page\": 25, \"index\": 2, \"width\": 1022, \"height\": 490}, {\"url\": \"assets/figures/arxiv/2605.00237v1/fig-003.webp\", \"caption\": \"Figure 5: The smallest value of the Automotive function observed for (a) DiceOptim and (b) TreeBO. The faint lines represent individual runs while the bold lines represent the mean of all runs. For each method, the standard deviation of the runs is added to and subtracted from the mean line to create a shaded region around the mean. The panels show that DiceOptim performs worse than TreeBO on average, although with smaller variance. The higher variance of TreeBO is primarily attributable to one outlier run that performs poorly. The mean runtimes for the two methods (not shown) were approximately 14 days for DiceOptim, and 9 days for TreeBO.\", \"page\": 15, \"index\": 3, \"width\": 1022, \"height\": 445}, {\"url\": \"assets/figures/arxiv/2605.00237v1/fig-004.webp\", \"caption\": \"Figure 16: The delta between the smallest value of the Levy function observed for DiceOptim and TreeBO. The vertical axis is on a symmetric-logarithmic scale. For each pair of runs, the smallest value observed for DiceOptim is subtracted from the smallest value observed for TreeBO. The faint lines represent the deltas for individual pairs of runs while the bold line represents the mean of all deltas. The standard deviation of the deltas is added to and subtracted from the mean line to create a shaded region around the mean. The plot shows that DiceOptim outperforms TreeBO, on average, until approximately 20 subsequent observations. From that point on, TreeBO outperforms DiceOptim. The plot also shows that TreeBO outperforms DiceOptim on the vast majority of runs, as indicated by the paucity of lines above the dashed line at 0 on the vertical axis.\", \"page\": 21, \"index\": 4, \"width\": 1022, \"height\": 490}, {\"url\": \"assets/figures/arxiv/2605.00237v1/fig-005.webp\", \"caption\": \"Figure 17: The smallest value of the Michalewicz function observed for (a) DiceOptim and (b) TreeBO. The faint lines represent individual runs while the bold lines represent the mean of all runs. For each method, the standard deviation of the runs is added to and subtracted from the mean line to create a shaded region around the mean. The panels show that DiceOptim performs worse than TreeBO, on average. However, DiceOptim outperforms TreeBO for most of the duration, especially during the first 100 subsequent observations. It is only at roughly 250 subsequent observations that TreeBO improves markedly and all the runs descend below −8 on the vertical axis. From Table 1 in the paper, for this test ninit = 100 and nnode = 350, so this marked improvement at 250 subsequent observations coincides with the first partition by TreeBO of the domain of the Michalewicz function.\", \"page\": 21, \"index\": 5, \"width\": 1022, \"height\": 446}, {\"url\": \"assets/figures/arxiv/2605.00237v1/fig-006.webp\", \"caption\": \"Figure 1: The binary partitioning in TreeBO proceeds via clustering and non-linear classification, enabling the optimization method to home in on the global minimum of the objective function. (a) Surface plot of the 2-dimensional Rastrigin test function, which is defined in Section 4. The function is highly multimodal but gradually slopes down towards its minimum value, 0, at the origin. Given the goal of minimizing the Rastrigin function, a flexible partitioning method should carve out a subregion focused on the origin. (b) One-hundred initial points are spread over the domain. Clustering on the (x, f(x)) pairs leads to binary labels for the dataset, shown as purple and yellow points. The labels and data are then used to fit a nonlinear binary classifier whose predictions implicitly partition the domain. The inner yellow subregion is focused on a neighborhood of the argument of the minimum, denoted by the red dot. Each subregion (node) is given its own Gaussian process model for f , fit using the points within the subregion along with nearby supplementary points. Each subregion is then given its own acquisition function, which is defined so that the subregion bounds are respected during its optimization. The algorithm takes the next observation of f in the most promising subregion, as determined by the acquisition functions. See Algorithm 1 and Section 3 for details.\", \"page\": 2, \"index\": 6, \"width\": 995, \"height\": 572}, {\"url\": \"assets/figures/arxiv/2605.00237v1/fig-007.webp\", \"caption\": \"Figure 19: Side-by-side boxplots of the results for the Automotive test function in 124 dimensions. The boxplots show the smallest objective-function value found, for both DiceOptim and TreeBO, at several points over the optimization process. Each boxplot represents the distribution over all 10 paired runs. At 0 subsequent observations the distributions are identical for both methods, because both methods are given identical sets of 125 initial points for each paired run. The boxplots show that DiceOptim outperforms TreeBO until roughly 300 subsequent observations. From that point on, TreeBO outperforms DiceOptim. The final pair of boxplots at 475 subsequent observations shows that the 75th percentile for TreeBO is less than the 25th percentile for DiceOptim.\", \"page\": 22, \"index\": 7, \"width\": 1022, \"height\": 489}, {\"url\": \"assets/figures/arxiv/2605.00237v1/fig-008.webp\", \"caption\": \"Figure 18: The delta between the smallest value of the Michalewicz function observed for DiceOptim and TreeBO. For each pair of runs, the smallest value observed for DiceOptim is subtracted from the smallest value observed for TreeBO. The faint lines represent the deltas for individual pairs of runs while the bold line represents the mean of all deltas. The standard deviation of the deltas is added to and subtracted from the mean line to create a shaded region around the mean. The plot shows that DiceOptim outperforms TreeBO, on average, until approximately 300 subsequent observations. From that point on, TreeBO outperforms DiceOptim. The delta between the two methods noticeably decreases in favor of TreeBO at 250 subsequent observations, which coincides with the first partition by TreeBO of the domain of the Michalewicz function. This is because, as shown in Table 1 in the paper, for this test ninit = 100 and nnode = 350, so the first split occurs at 250 subsequent observations.\", \"page\": 22, \"index\": 8, \"width\": 1012, \"height\": 486}, {\"url\": \"assets/figures/arxiv/2605.00237v1/fig-009.webp\", \"caption\": \"Figure 10: The delta between the smallest value of the Hartmann function observed for DiceOptim and TreeBO. For each pair of runs, the smallest value observed for DiceOptim is subtracted from the smallest value observed for TreeBO. The faint lines represent the deltas for individual pairs of runs while the bold line represents the mean of all deltas. The standard deviation of the deltas is added to and subtracted from the mean line to create a shaded region around the mean. The plot shows that DiceOptim outperforms TreeBO, on average, until approximately 20 subsequent observations. From then until 100 subsequent observations there is little apparent difference between the two methods. However, from roughly 100 subsequent observations on, TreeBO locates the global minimum on several runs while DiceOptim fails to do so. This is indicated by the descent of several faint lines below the dashed line at 0.0 on the vertical axis.\", \"page\": 18, \"index\": 9, \"width\": 1022, \"height\": 490}, {\"url\": \"assets/figures/arxiv/2605.00237v1/fig-010.webp\", \"caption\": \"Figure 20: Side-by-side boxplots of the results for the Ackley test function in 6 dimensions. The boxplots show the smallest objectivefunction value found, for both DiceOptim and TreeBO, at several points over the optimization process. Each boxplot represents the distribution over all 100 paired runs. At 0 subsequent observations the distributions are identical for both methods, because both methods are given identical sets of 60 initial points for each paired run. The boxplots show that both methods perform similarly through 75 subsequent observations. However, from that point on, TreeBO substantially outperforms DiceOptim. The final pair of boxplots at 140 subsequent observations shows that TreeBO almost always discovers the global minimum, whereas DiceOptim only does so on less than half of all runs.\", \"page\": 23, \"index\": 10, \"width\": 1022, \"height\": 489}, {\"url\": \"assets/figures/arxiv/2605.00237v1/fig-011.webp\", \"caption\": \"Figure 21: Side-by-side boxplots of the results for the Hartmann test function in 6 dimensions. The boxplots show the smallest objectivefunction value found, for both DiceOptim and TreeBO, at several points over the optimization process. Each boxplot represents the distribution over all 100 paired runs. At 0 subsequent observations the distributions are identical for both methods, because both methods are given identical sets of 60 initial points for each paired run. The boxplots do not show meaningful differences between the two methods. See Figures 9 and 10 for alternative plots that do show TreeBO outperforming DiceOptim on the Hartmann test function.\", \"page\": 23, \"index\": 11, \"width\": 1022, \"height\": 489}, {\"url\": \"assets/figures/arxiv/2605.00237v1/fig-012.webp\", \"caption\": \"Figure 3: Side-by-side boxplots of the results for the Ackley test function in 6 dimensions. The boxplots show the smallest objectivefunction value found, for both DiceOptim and TreeBO, at several points over the optimization process. Each boxplot represents the distribution over all 100 paired runs. At zero subsequent observations the distributions are identical for both methods, because both methods are given identical sets of 60 initial points for each paired run. The results show that DiceOptim is frequently unable to home in on a neighborhood of the arguments of the minima, where the objective-function values significantly decrease towards the global minimum, zero. In contrast, TreeBO almost always discovers the global minimum, as evidenced by the near disappearance of the red boxplot after 140 subsequent observations.\", \"page\": 8, \"index\": 12, \"width\": 498, \"height\": 341}, {\"url\": \"assets/figures/arxiv/2605.00237v1/fig-013.webp\", \"caption\": \"Figure 12: The delta between the smallest value of the Rastrigin function observed for DiceOptim and TreeBO. The vertical axis is on a symmetric-logarithmic scale. For each pair of runs, the smallest value observed for DiceOptim is subtracted from the smallest value observed for TreeBO. The faint lines represent the deltas for individual pairs of runs while the bold line represents the mean of all deltas. The standard deviation of the deltas is added to and subtracted from the mean line to create a shaded region around the mean. The plot shows that DiceOptim outperforms TreeBO, on average, until approximately 150 subsequent observations. From that point on, TreeBO outperforms DiceOptim. For the majority of runs the delta between the methods is approximately ±1 since one method or the other finds a better local minimum. The the plot shows that −1 is more common than 1, which is consistent with the superiority of TreeBO over DiceOptim for this test.\", \"page\": 19, \"index\": 13, \"width\": 1022, \"height\": 490}, {\"url\": \"assets/figures/arxiv/2605.00237v1/fig-014.webp\", \"caption\": \"Figure 8: The delta between the smallest value of the Ackley function observed for DiceOptim and TreeBO. For each pair of runs, the smallest value observed for DiceOptim is subtracted from the smallest value observed for TreeBO. The faint lines represent the deltas for individual pairs of runs while the bold line represents the mean of all deltas. The standard deviation of the deltas is added to and subtracted from the mean line to create a shaded region around the mean. The plot shows that DiceOptim outperforms TreeBO, on average, until approximately 70 subsequent observations. From that point on, TreeBO substantially outperforms DiceOptim. The plot also shows that TreeBO outperforms DiceOptim on the vast majority of runs. The mean delta between the two methods noticeably decreases in favor of TreeBO starting at 40 subsequent observations, which coincides with the first partition by TreeBO of the domain of the Ackley function. This is because, as shown in Table 1 in the paper, for this test ninit = 60 and nnode = 100, so the first split occurs at 40 subsequent observations.\", \"page\": 17, \"index\": 14, \"width\": 1022, \"height\": 490}, {\"url\": \"assets/figures/arxiv/2605.00237v1/fig-015.webp\", \"caption\": \"Figure 22: Side-by-side boxplots of the results for the Rastrigin test function in 6 dimensions. The vertical axis is on a logarithmic scale. The boxplots show the smallest objective-function value found, for both DiceOptim and TreeBO, at several points over the optimization process. Each boxplot represents the distribution over all 100 paired runs. At 0 subsequent observations the distributions are identical for both methods, because both methods are given identical sets of 60 initial points for each paired run. The boxplots show very little difference between the two methods until roughly 200 subsequent observations. From that point on, TreeBO outperforms DiceOptim. The final pair of boxplots at 340 subsequent observations shows that the median for DiceOptim is roughly 2, while the median for TreeBO is roughly 1.\", \"page\": 24, \"index\": 15, \"width\": 1022, \"height\": 489}, {\"url\": \"assets/figures/arxiv/2605.00237v1/fig-016.webp\", \"caption\": \"Figure 23: Side-by-side boxplots of the results for the Schwefel test function in 6 dimensions. The boxplots show the smallest objectivefunction value found, for both DiceOptim and TreeBO, at several points over the optimization process. Each boxplot represents the distribution over all 100 paired runs. At 0 subsequent observations the distributions are identical for both methods, because both methods are given identical sets of 60 initial points for each paired run. The boxplots show that TreeBO outperforms DiceOptim from beginning to end, although the gap between the two methods is larger at the beginning and end of the optimization process than in the middle. The final pair of boxplots at 340 subsequent observations shows that the 25th percentile for TreeBO is much lower than for DiceOptim. This indicates that TreeBO more frequently finds smaller local minima of the Schwefel function than does DiceOptim, although both methods struggle to locate the global minimum of 0.\", \"page\": 24, \"index\": 16, \"width\": 1022, \"height\": 489}, {\"url\": \"assets/figures/arxiv/2605.00237v1/fig-017.webp\", \"caption\": \"Figure 4: Side-by-side boxplots of the results for the Automotive test function in 124 dimensions. The boxplots show the smallest objective-function value found, for both DiceOptim and TreeBO, at several points over the optimization process. Each boxplot represents the distribution over all 10 paired runs. At zero subsequent observations the distributions are identical for both methods, because both methods are given identical sets of 125 initial points for each paired run. The results show that DiceOptim leads during the first half of the optimization process, after which TreeBO gradually gains ground and eventually finishes ahead. In addition to superior optimization performance, the mean runtime for TreeBO was approximately 9 days, compared to approximately 14 days for DiceOptim. This reduction in runtime is primarily because TreeBO fits Gaussian process models for f with at most 375 data points, whereas DiceOptim fits models with all available data points—eventually 600.\", \"page\": 9, \"index\": 17, \"width\": 498, \"height\": 342}, {\"url\": \"assets/figures/arxiv/2605.00237v1/fig-018.webp\", \"caption\": \"Table 1: Test Results for TreeBO and DiceOptim. TreeBO Obtains Lower Average Values for all Tests.\", \"page\": 9, \"index\": 18, \"width\": 1076, \"height\": 208}, {\"url\": \"assets/figures/arxiv/2605.00237v1/fig-019.webp\", \"caption\": \"Figure 14: The delta between the smallest value of the Schwefel function observed for DiceOptim and TreeBO. For each pair of runs, the smallest value observed for DiceOptim is subtracted from the smallest value observed for TreeBO. The faint lines represent the deltas for individual pairs of runs while the bold line represents the mean of all deltas. The standard deviation of the deltas is added to and subtracted from the mean line to create a shaded region around the mean. The plot shows that TreeBO outperforms DiceOptim, on average, over the entire optimization process. The delta between the two methods peaks at roughly 40 subsequent observations before receding until 200 subsequent observations, and then grows again from that point on.\", \"page\": 20, \"index\": 19, \"width\": 1022, \"height\": 490}, {\"url\": \"assets/figures/arxiv/2605.00237v1/fig-020.webp\", \"caption\": \"Figure 6: The delta between the smallest value of the Automotive function observed for DiceOptim and TreeBO. For each pair of runs, the smallest value observed for DiceOptim is subtracted from the smallest value observed for TreeBO. The faint lines represent the deltas for individual pairs of runs while the bold line represents the mean of all deltas. The standard deviation of the deltas is added to and subtracted from the mean line to create a shaded region around the mean. The plot shows that DiceOptim outperforms TreeBO, on average, until approximately 360 subsequent observations. From that point on, TreeBO outperforms DiceOptim. The plot also shows that TreeBO outperforms DiceOptim on 8 of 10 runs.\", \"page\": 16, \"index\": 20, \"width\": 1022, \"height\": 490}, {\"url\": \"assets/figures/arxiv/2605.00237v1/fig-021.webp\", \"caption\": \"Figure 7: The smallest value of the Ackley function observed for (a) DiceOptim and (b) TreeBO. The faint lines represent individual runs while the bold lines represent the mean of all runs. For each method, the standard deviation of the runs is added to and subtracted from the mean line to create a shaded region around the mean. The panels show that DiceOptim performs worse than TreeBO on average, and with larger variance. Many of the individual runs for DiceOptim are unable to locate the region of steep descent to the global minimum near the origin, and so struggle to locate values of the objective function that are less than 5 or 10. In contrast, TreeBO accomplishes this on all but 4 runs out of 100. Also, the slope of the mean line for TreeBO noticeably decreases at roughly 40 subsequent observations. From Table 1 in the paper, in this test ninit = 60 and nnode = 100, so this improvement in performance coincides with the first partition by TreeBO of the domain of the Ackley function.\", \"page\": 16, \"index\": 21, \"width\": 1022, \"height\": 445}]"
motivation: 传统贝叶斯优化在处理大规模数据时因立方复杂度而计算不可行，并且全局建模不利于局部最小化。
method: 作者采用灵活的递归二叉空间划分算法并结合局部高斯过程模型以实现线性时间的贝叶斯优化。
result: 该方法在7个维度从6到124的复杂测试函数上表现优于常用的贝叶斯优化库。
conclusion: 该方法在保持高效优化性能的同时显著降低计算复杂度，展示了实用潜力。
---

## 摘要
贝叶斯优化是一种顺序方法，用于最小化评估代价高昂且可假设条件很少的目标函数。通过利用所有已收集的数据来训练目标函数的高斯过程模型，并自适应地结合全局探索与局部开发，该方法已广泛应用于包括机器学习、汽车工程和强化学习在内的多个领域的优化任务中。然而，标准方法存在两个问题：1）由于训练集规模的三次计算复杂度，模型训练最终会变得在计算上不可行；2）考虑到最小化过程的局部特性，对目标函数进行全局建模并不一定是最优的。我们采用对搜索空间的灵活且递归的二元划分策略，使标准贝叶斯优化的建模和采集部分都能与划分方案和谐配合，从而改善上述两项缺陷。我们在维度从 6 到 124 的七个具有挑战性的测试函数上，将我们的方法与一个常用的贝叶斯优化库进行了对比，结果表明，我们的方法在所有测试中均实现了更优的优化性能。此外，我们的方法具有线性计算复杂度。

## Abstract
Bayesian optimization is a sequential method for minimizing objective functions that are expensive to evaluate and about which few assumptions can be made. By using all gathered data to train a Gaussian process model for the function and adaptively employing a mixture of global exploration and local exploitation, this method has been used for optimization in many fields including machine learning, automotive engineering and reinforcement learning. However, the standard method suffers from two problems: 1) with cubic computational complexity in the training-set size it eventually becomes computationally infeasible to train the model, and 2) globally modeling the objective function is not necessarily optimal given the local nature of minimization. Using flexible and recursive binary partitioning of the search space, we adapt both the modeling and acquisitive aspects of standard Bayesian optimization to work harmoniously with the partitioning scheme, thereby ameliorating both standard shortcomings. We compare our method against a commonly used Bayesian optimization library on seven challenging test functions, ranging in dimensionality from $6$ to $124$, and show that our method achieves superior optimization performance in all tests. In addition our method has linear computational complexity.