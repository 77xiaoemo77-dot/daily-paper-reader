---
title: "QUIVER: Cost-Aware Adaptive Preference Querying in Surrogate-Assisted Evolutionary Multi-Objective Optimization"
title_zh: QUIVER：在代理辅助的进化多目标优化中具备成本意识的自适应偏好查询
authors: Florian A. D. Burnat
date: 2026-05-05
pdf: "https://arxiv.org/pdf/2605.04267v1"
tags: ["query:optai"]
score: 9.0
evidence: 带有偏好查询的代理辅助进化多目标优化器
tldr: 本文提出QUIVER算法，用于在代理辅助的多目标进化优化中实现成本感知的自适应偏好查询。该方法在有限资源下权衡目标函数评估与决策者偏好获取，通过最大化单位成本的期望决策质量提升，动态选择查询类型，显著降低最终效用遗憾并提高查询效率。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.04267v1/fig-001.webp\", \"caption\": \"Figure 3: Cost sensitivity analysis on DTLZ2 (𝑚 = 3). QUIVER gradually reduces IA usage as the cost ratio increases, demonstrating cost-aware modality selection.\", \"page\": 16, \"index\": 1, \"width\": 681, \"height\": 468}, {\"url\": \"assets/figures/arxiv/2605.04267v1/fig-002.webp\", \"caption\": \"Figure 2: Budget allocation for QUIVER across benchmarks. Evaluations dominate the cost budget (∼92%), with IA fraction among preference queries ranging from 20% (easy DTLZ2) to 35% (hard WFG9).\", \"page\": 15, \"index\": 2, \"width\": 694, \"height\": 468}, {\"url\": \"assets/figures/arxiv/2605.04267v1/fig-003.webp\", \"caption\": \"Table 2: Action counts for QUIVER (mean over 5 seeds, 𝐵 = 500). Shows the number of PS queries (n_ps), IA queries (n_ia), and IA fraction among preference queries. The policy adapts modality selection to problem difficulty.\", \"page\": 13, \"index\": 3, \"width\": 913, \"height\": 614}, {\"url\": \"assets/figures/arxiv/2605.04267v1/fig-004.webp\", \"caption\": \"Figure 1: Utility regret comparison across benchmarks. Preference-learning methods achieve near-zero regret while Eval-only has 23–34% regret.\", \"page\": 14, \"index\": 4, \"width\": 790, \"height\": 318}]"
motivation: 解决交互式多目标优化中资源在昂贵目标评估与偏好获取之间分配的难题。
method: 采用代理辅助进化优化框架，基于成本感知的期望决策改进准则在不同查询模式间自适应选择。
result: "在DTLZ和WFG基准上，QUIVER在复杂问题中取得最低效用遗憾，比单一查询模式方法提升约25%。"
conclusion: QUIVER实现了针对问题难度的自适应查询策略，有效平衡计算与偏好获取成本。
---

## 摘要
交互式多目标优化系统面临预算分配的两难境地：可以将资源用于昂贵的目标函数评估，也可以用于引导决策者偏好，以确定帕累托集的相关区域。此外，偏好获取本身具有不同模态、信息量和认知负担，从廉价但带噪声的成对偏好陈述（PS），到信息更丰富但成本更高的无差异调整（IA）。我们研究在未知标量化下的具成本意识的优化，并提出 QUIVER（Query-Informed Value Estimation for Regret），这是一种代理辅助的进化多目标优化器，能够在目标评估与异质偏好查询之间自适应选择。在每一步中，QUIVER 通过最大化每单位总成本下预期决策质量提升来选择下一步动作。在基于合成决策者模型的 DTLZ 和 WFG 基准测试中，QUIVER 在具有挑战性的 WFG 问题上实现了最低的最终效用遗憾（WFG4 上的效用遗憾为 2.14，WFG9 为 2.82，比基准提升 25%），优于所有单一模态基线。我们分析了 PS 与 IA 的最优混合如何随问题难度进行调整：在简单问题（DTLZ2）上，QUIVER 选择 80% 的 PS 查询；在困难问题（WFG9）上，则转向 35% 的 IA 查询。这种自适应模态选择展示了具成本意识的偏好学习在实际中的应用。

## Abstract
Interactive multi-objective optimization systems face a budget allocation dilemma: one can spend resources on expensive objective evaluations or on eliciting decision-maker preferences that identify the relevant region of the Pareto set. Moreover, preference elicitation itself spans modalities with different information content and cognitive burden, ranging from cheap, noisy pairwise preference statements (PS) to richer but costlier indifference adjustments (IA).   We study cost-aware optimization under an unknown scalarization and introduce QUIVER (Query-Informed Value Estimation for Regret), a surrogate-assisted evolutionary multi-objective optimizer that adaptively chooses between objective evaluations and heterogeneous preference queries. At each step, QUIVER selects the next action by maximizing the expected decision-quality improvement per unit total cost. Across DTLZ and WFG benchmarks under synthetic decision-maker models, QUIVER achieves the lowest final utility regret on challenging WFG problems (utility regret of 2.14 on WFG4, 2.82 on WFG9: a 25% improvement over baselines), outperforming all single-modality baselines. We analyze how the optimal mix of PS and IA adapts to problem difficulty: on easy problems (DTLZ2), QUIVER selects 80\% PS queries; on hard problems (WFG9), it shifts to 35% IA queries. This adaptive modality selection demonstrates cost-aware preference learning in action.

---

## 论文详细总结（自动生成）

## 一、研究背景与核心问题

- **研究动机**：  
  多目标优化（MOO）问题常涉及多个互相冲突的目标，如工程设计或投资组合优化。目标函数评估通常代价高昂，因此近年来出现了**代理辅助（surrogate-assisted）**方法来减少昂贵评估次数。  
  在这些系统中，最终方案的选择取决于人类决策者（DM）的**偏好**，但偏好往往未知。即便求得完整的帕累托前沿（Pareto front），若不了解 DM 偏好，也难以确定理想解。因而存在一个核心难题：**如何在有限预算下，在目标评估和偏好获取之间进行分配。**

- **核心问题**：  
  在有限的成本预算下，应如何权衡：
  1. **昂贵的目标函数评估（Eval）**；
  2. **廉价但信息有限的偏好查询（如成对比较 PS）**；
  3. **信息更丰富但认知成本更高的偏好查询（如无差异调整 IA）**。  
  作者提出的问题即是：**如何实现具“成本意识”（cost-aware）的自适应偏好查询策略，以最大化每单位成本获得的决策质量提升**。

---

## 二、方法论与技术细节

### 1. 总体框架

- 提出算法 **QUIVER（Query-Informed Value Estimation for Regret）**，其思想是将“目标评估”和“多种偏好查询”统一视作可选的**行动空间**，在每步中选择一个行动，使得**单位成本下的期望决策质量提升最大化**。
- 算法分为两层：
  - **搜索层（Search Layer）**：基于 NSGA-II 的进化优化器；
  - **控制层（Controller）**：计算每种动作的“信息价值（Value of Information, VOI）/成本”比值，决定执行 Eval、PS 或 IA。

### 2. 偏好建模与估计

- 偏好表示为一个线性效用模型：  
  \[
  u(f(x); w) = w^\top f(x), \quad w \in \Delta_{m-1}
  \]
  其中 \( w \) 是未知的偏好权重向量。

- **PS 模型（Pairwise Statement）**：采用 Bradley–Terry/Logit 模型，表示“更偏好 A 还是 B”的二元比较：
  \[
  P(y=1 | A, B, w) = \sigma(\beta[u(A;w)-u(B;w)])
  \]

- **IA 模型（Indifference Adjustment）**：询问 DM 在某一维上需调整多少值才能令两方案无差异，建模为带噪的高斯分布，有效反映决策者的边际替代率。

- **后验更新**：通过粒子滤波（Sequential Monte Carlo, SMC）维护偏好后验 \(p(w | \mathcal{D}_{pref})\)，用加权样本近似更新。

### 3. 动作选择原则

- 对三类动作（Eval、PS、IA），定义期望决策质量提升：
  \[
  a^* = \arg\max_a \frac{E[\Delta Q | a]}{c(a)}
  \]
  其中 \(\Delta Q\) 表示熵减少或期望效用增益，\(c(a)\) 为动作成本。

- **偏好查询的 VOI**：用后验熵减少表示信息增益，IA 的连续响应离散化成 15 个区间以避免信息量虚增。

- **评估动作的 VOI**：用期望效用改善（Value of Clairvoyance）衡量。

- **查询对选择策略**：选择后验不确定性（分歧最大）的解对（posterior disagreement criterion），IA 还会选取方差最高的目标维度。

### 4. 算法概述（文字版）

1. 初始化人群与少量评估；
2. 更新代理模型与候选集；
3. 计算每个动作的 VOI/成本；
4. 执行动作（Eval 或偏好查询）；
5. 更新偏好后验与进化人群；
6. 根据偏好后验期望效用选出最终解。

---

## 三、实验设计

- **基准任务（Benchmarks）**：
  - DTLZ2（3 与 5 目标）：光滑、容易收敛；
  - WFG4（3 目标）：多峰、分离；
  - WFG9（3 目标）：非分离、欺骗性强、最困难。

- **成本设定**：  
  \(c_{eval}=5.0\)、\(c_{PS}=1.0\)、\(c_{IA}=1.15\)（来源于认知实验，IA 比 PS 成本高 15%）。

- **总预算**：\(B=500\)（约 100 次评估或 435 次 PS）。

- **比较基线**：
  1. Eval-only（纯目标评估）；
  2. PS-only；
  3. IA-only；
  4. Fixed-schedule（固定节奏交替）；
  5. Random（按成本逆比随机选择）。  
  均基于相同的 NSGA-II 主干实现，确保公平。

- **算法参数**：  
  种群大小 20；偏好粒子数 2048；蒙特卡洛样本 50；所有实验运行 5 个随机种子。

- **度量指标**：
  - 效用遗憾（Utility Regret）；
  - 查询类型比例；
  - 偏好后验熵（不确定性）。

---

## 四、资源与算力

- 论文未报告具体计算硬件、GPU 型号或训练时间。  
  推测实验为**仿真型优化**而非神经网络训练，因此计算开销主要来自蒙特卡洛与进化操作，无需高性能 GPU。  
  文中指出算法的 VOI 估计成本约为 \(O(M \times S) = 10^5\) 级操作／步，远低于实际评估开销。

---

## 五、实验数量与充分性

- 实验覆盖：
  - 三类基准（DTLZ2、WFG4、WFG9）；
  - 成本比敏感性实验；
  - 动态疲劳成本实验；
  - 五种对照基线；
  - 每组 5 次独立运行。  
  共约十余组实验，包含不同问题维度、成本设定与动态成本场景，具备较强的全面性。

- 公平性：  
  所有方法共享相同的优化主干和总预算控制，结果按相同指标评测，设计较为客观。

---

## 六、主要结论与发现

- **性能提升**：  
  QUIVER 在最困难的 WFG9 上效用遗憾降至 2.82，比固定或单一查询模式低约 25%。

- **自适应行为**：  
  - 简单问题（DTLZ2）：偏重廉价 PS（约 80%）。  
  - 困难问题（WFG9）：转向 35% IA 查询，获得更多信息。

- **成本敏感性**：  
  当 IA/PS 成本比上升时，算法自动减少 IA 使用，展现合理的成本感知特性。

- **动态代价场景（疲劳）**：  
  随着 IA 成本递增，QUIVER自动减少 IA 次数，且效用损失平缓，表现出良好稳健性。

---

## 七、优点与创新点

- **统一决策框架**：  
  将评估与偏好查询统一在“动作—成本—信息增益”范式下，首次实现跨模态预算分配的可行策略。

- **VOI-per-cost 原则可解释**：  
  给出了明确的模态切换阈值 \(IG_{IA}/IG_{PS} > c_{IA}/c_{PS}\)，便于理解和分析。

- **偏好粒子滤波推断**：  
  较传统 MCMC 更高效，能在线连续更新，不增加显著开销。

- **高鲁棒性与扩展性**：  
  控制层独立于优化器，可应用于其他进化算法、Bayesian Optimization 或 RLHF 框架。

- **实验系统性强**：  
  各实验清晰展示算法在不同难度、成本条件下的行为变化与性能趋势。

---

## 八、不足与局限

- **偏好模型假设简化**：  
  所有实验均假定线性效用（加权求和），无法捕捉现实中非凸偏好或非线性效用。

- **合成决策者假设**：  
  DM 响应由噪声模型模拟，缺乏真实用户实验，可能高估算法稳定性。

- **搜索空间局限**：  
  仅测试了低维 DTLZ/WFG 问题，未检验在高维目标或复杂约束问题中的可扩展性。

- **成本模型简化**：  
  认知成本设为固定常数，尽管疲劳实验有体现动态变化，仍未考虑个体差异或任务依赖性。

- **缺少跨方法比较**：  
  未与其他真正交互式 EMO 框架（如 NAUTILUS / NIMBUS）比较，外部有效性待验证。

---

**（完）**
