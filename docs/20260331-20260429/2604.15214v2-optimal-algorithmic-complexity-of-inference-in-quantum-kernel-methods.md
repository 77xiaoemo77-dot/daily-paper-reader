---
title: Optimal algorithmic complexity of inference in quantum kernel methods
title_zh: 量子核方法中推理的最优算法复杂度
authors: "Elies Gil-Fuster, Seongwook Shin, Sofiene Jerbi, Jens Eisert, Maximilian J. Kramer"
date: 2026-04-16
pdf: "https://arxiv.org/pdf/2604.15214v2"
tags: ["query:qmlfin"]
score: 8.0
evidence: 量子核方法用于监督学习
tldr: "本文研究量子核方法中的推理阶段成本问题，通过系统分析核函数估计与求和方式的组合，提出一种将推理过程编码为单一可观测量并使用量子幅度估计的新算法，实现查询复杂度 O(||α||₁/ε)，显著优于传统方法，并给出匹配下界及实际硬件的权衡分析。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.15214v2/fig-001.webp\", \"caption\": \"FIG. 2. Realizing the “training-set oracle” O†(S) in Definition 6, as N controlled calls to the inverse of the single-input oracle U(x), one for each input in the training set xi ∈ S. The associated gate cost is O(N(G + log(N))). This construction resembles ideas in the linear combination of unitaries (LCU) framework [CW12].\", \"page\": 6, \"index\": 1, \"width\": 980, \"height\": 209}, {\"url\": \"assets/figures/arxiv/2604.15214v2/fig-002.webp\", \"caption\": \"FIG. 1. Schematic overview of the four algorithmic strategies for inference in quantum kernel methods. The two axes correspond to the two independent design choices: how the sum over training points is approximated (list-and-sum versus all-at-once, vertical axis), and how individual expectation values are estimated (sampling versus quantum amplitude estimation, horizontal axis). In list-and-sum, each kernel value k(x, xi) is estimated independently and the results are combined classically. In the all-at-once approach, the full inference sum is encoded as a single observable. Moving from sampling to quantum amplitude estimation yields a quadratic improvement in precision scaling within each row.\", \"page\": 2, \"index\": 2, \"width\": 990, \"height\": 603}, {\"url\": \"assets/figures/arxiv/2604.15214v2/fig-003.webp\", \"caption\": \"FIG. 4. Quantum circuit encoding the all-at-once observable as amplitudes. Explicit implementation of the unitary V described in Lemma 8.\", \"page\": 7, \"index\": 3, \"width\": 990, \"height\": 252}, {\"url\": \"assets/figures/arxiv/2604.15214v2/fig-004.webp\", \"caption\": \"FIG. 3. Quantum circuit encoding the all-at-once observable as a single expectation value. The all-at-once observable M̃(α, S) is the collection of gates and fixed observables inside the box, up to a factor of ∥α∥1. The intermediate-step formulas serve the purpose of illustrating the action of the gates onto the input state vector |ϕ(x)⟩.\", \"page\": 7, \"index\": 4, \"width\": 1052, \"height\": 226}]"
motivation: 当前量子核方法在推理阶段需估计大量核值，计算成本高，限制其实用性。
method: 将推理求和过程编码为单一可观测量并结合量子幅度估计进行估算。
result: "研究提出一种查询复杂度最优的量子核方法推理算法，将复杂度降至 O(||α||₁/ε)，并证明其最优性。"
conclusion: 研究确立了量子核方法推理的查询复杂度最优策略，并提供了实际实现的指导。
---

## 摘要
量子核方法是实现量子监督学习优势的主要候选方案之一。其关键瓶颈在于推理的成本：在新数据上评估已训练模型需要以加性精度 $\varepsilon$ 估计加权和 $\sum_{i=1}^N α_i k(x,x_i)$，其中 $α$ 是训练得到的系数向量。标准方法通过独立采样估计每一项，其查询复杂度为 $O(N\lVertα\rVert_2^2/\varepsilon^2)$。在本工作中，我们识别了两个可独立改进的维度：（1）单个核值的估计方式（采样与量子振幅估计），以及（2）求和的近似方式（逐项估计与单一可观测量表示），并系统地分析了所有组合。查询最优的组合方案将整个推理求和编码为单一可观测量的期望值并应用量子振幅估计，其查询复杂度达到 $O(\lVertα\rVert_1/\varepsilon)$，从查询次数中消除了对 $N$ 的依赖，并在 $\lVertα\rVert_1$ 和 $\varepsilon$ 上实现了双重平方级改进。我们证明了匹配的下界 $Ω(\lVertα\rVert_1/\varepsilon)$，从而在对数因子范围内确立了我们方法的查询最优性。除了查询复杂度，我们还分析了这些改进对量子门成本的影响，表明从门复杂度角度看，查询最优策略在实际中并不总是最佳。我们的结果同时提供了查询最优的算法以及根据硬件能力选择的实际最优策略，并给出了完整的中间方法谱以供实践者参考。所有算法仅需以振幅估计作为子程序，因此是早期容错实现的自然候选方案。

## Abstract
Quantum kernel methods are among the leading candidates for achieving quantum advantage in supervised learning. A key bottleneck is the cost of inference: evaluating a trained model on new data requires estimating a weighted sum $\sum_{i=1}^N α_i k(x,x_i)$ of $N$ kernel values to additive precision $\varepsilon$, where $α$ is the vector of trained coefficients. The standard approach estimates each term independently via sampling, yielding a query complexity of $O(N\lVertα\rVert_2^2/\varepsilon^2)$. In this work, we identify two independent axes for improvement: (1) How individual kernel values are estimated (sampling versus quantum amplitude estimation), and (2) how the sum is approximated (term-by-term versus via a single observable), and systematically analyze all combinations thereof. The query-optimal combination, encoding the full inference sum as the expectation value of a single observable and applying quantum amplitude estimation, achieves a query complexity of $O(\lVertα\rVert_1/\varepsilon)$, removing the dependence on $N$ from the query count and yielding a quadratic improvement in both $\lVertα\rVert_1$ and $\varepsilon$. We prove a matching lower bound of $Ω(\lVertα\rVert_1/\varepsilon)$, establishing query-optimality of our approach up to logarithmic factors. Beyond query complexity, we also analyze how these improvements translate into gate costs and show that the query-optimal strategy is not always optimal in practice from the perspective of gate complexity. Our results provide both a query-optimal algorithm and a practically optimal choice of strategy depending on hardware capabilities, along with a complete landscape of intermediate methods to guide practitioners. All algorithms require only amplitude estimation as a subroutine and are thus natural candidates for early-fault-tolerant implementations.